# rtt index | generated 2026-09-18 15:28 | 81 files | 28906 tokens
# If this looks outdated, ask the user to run: rtt update
# agente_pesagem/agente_pesagem.py [python]
imports: json, os, queue, re, select, socket, sys, threading, time, tkinter, io.BytesIO, datetime.datetime, pathlib.Path, tkinter.messagebox, tkinter.ttk, tkinter.scrolledtext.ScrolledText, requests, pesagem_calc.bruto_da_leitura, pesagem_calc.calcular_pesos, pesagem_calc.parse_peso_digitado, pesagem_calc.pesos_ja_pesado
def app_dir() -> Path
def user_config_dir() -> Path  # Pasta gravável do usuário — a URL do servidor sobrevive à troca do .exe.
def user_config_path() -> Path
def bundled_config_path() -> Path
def _bundle_dir() -> Path
def _ico_paths() -> list[Path]
def _aplicar_icone(win) -> None  # Ícone São Geraldo no .exe, barra de título, config e barra de tarefas.
def _resumo_http_erro(resp, url: str) -> str  # Status HTTP sem despejar HTML do Flask (404 em inglês confundia o log).
def default_config() -> dict
def _host_de_url(url: str) -> str
def _ler_json_config(path: Path) -> dict
def load_config() -> dict
def probe_tcp(ip: str, port: int, timeout: float = 1.8) -> tuple[str, str]  # Tenta TCP em ip:port. Retorna (codigo, mensagem_pt). codigo: aberta|recusada|tim
def portas_para_testar(preferida: int) -> list[int]
def save_config(cfg: dict) -> Path  # Grava no AppData (sempre) e tenta copiar ao lado do .exe.
def listar_portas_detalhe() -> list[tuple[str, str]]  # Retorna [(COMx, descricao), ...] incluindo serial nativa e USB.
def listar_portas() -> list[str]
def detectar_porta(preferida: str) -> str | None
def _preview_bytes(data: bytes, limite: int = 200) -> str
def _log_rx(data: bytes, limite: int = 200) -> str  # Log imediato: recebido N bytes hex=... ascii=...
def _unwrap_connect_frames(data: bytes) -> list[bytes]  # Extrai payload de cabeçalho length-prefixed (2 ou 4 bytes, BE/LE) se fizer senti
def _peel_length_frames(buf: bytearray) -> list[bytes]  # Consome frames length-prefixed completos de um buffer TCP.
def _bytes_para_texto(data: bytes) -> str
def _comandos_protocolo(cfg: dict) -> list[bytes]
def _rotulo_protocolo(cfg: dict) -> str
def _aplicar_unidade(peso: float, unit: str | None, sign: str | None) -> float
def _peso_de_digitos(digits: str) -> float | None  # Campo só numérico (Prot 4 / STX…ETX sem ponto). BA37 visor 000.00.
def _leitura(bruto: str, estavel: bool = False, **kw) -> dict
def _float_grupo(m) -> float | None
def parse_leitura(linha: str) -> dict  # Extrai peso líquido, bruto e tara do telegrama da balança.
def parse_peso(linha: str) -> tuple[float | None, str, bool]
def formatar_peso_ui(peso: float | None) -> str  # Somente o visor: 3 dígitos inteiros + 2 decimais (000.00). Não altera o valor gr
def formatar_peso_lista(peso: float | None) -> str  # Peso compacto na lista Pesados (65 kg / 12.50 kg).
class LedPesoDisplay(tk.Frame)
  def __init__(self, parent)
  def set_peso(self, peso: float | None, aceso: bool = True)
  def _redraw(self, _evt=None)
  def _draw_digit(self, c: tk.Canvas, x: float, y: float, w: float, h: float, ch: str, on: str)
  def _hseg(x, y, w, t)
  def _vseg(x, y, t, h)
def sunken(parent, **kw)
class BalancaReader(threading.Thread)
  def __init__(self, cfg: dict, out_q: queue.Queue, stop_event: threading.Event)
  def run(self)
  def _run_serial(self)
  def _run_tcp(self)
  def _run_tcp_paralelo(self, ip: str, port: int)  # Spec 167 = 33582 (cliente). Tenta em paralelo se 33581 aceitar TCP sem bytes.
  def _abrir_udp(self, porta: int) -> socket.socket | None
  def _handshake_connect_udp(self, ip: str, socks: list[tuple[int, socket.socket]])  # Um anúncio UDP no listen da balança (spec 168=33583). Sem ENQ em loop.
  def _run_udp_listen(self, ip: str)  # Escuta contínua UDP 33583 e 33584 (manual BA37 spec 168/169). Print pode ir UDP.
  def _run_tcp_listen(self)  # BA37 spec 43=2 modo cliente: a balança conecta neste PC (spec 167 = 33582).
  def _loop_bytes_socket(self, sock: socket.socket, origem: str)
  def _tratar_bytes(self, raw_bytes: bytes, origem: str) -> None  # Qualquer datagrama/byte: log imediato + parse de peso (ASCII e frame length-pref
  def _loop_bytes(self, origem: str, write_fn, read_fn, eof_raises: bool = False, modo_tcp: bool = False)
  def _emit(self, linha: str, porta: str) -> bool
  def _run_sim(self)
class JaPesadoDialog(tk.Toplevel)
  def __init__(self, master, tara_inicial=0.0)
  def _ok(self)
class PesoRecebidoDialog(tk.Toplevel)
  def __init__(self, master)
class EditarPesadoDialog(tk.Toplevel)
  def __init__(self, master, item: dict, labels: tuple, cliente_por_label: dict, placeholder: str)
  def _ok(self)
class ConfigDialog(tk.Toplevel)
  def __init__(self, master, cfg: dict, on_save)
  def _section(self, parent, title)
  def _field(self, parent, label, value)
  def _mostrar_conexao(self)
  def _limpar_indicador(self)
  def _set_indicador(self, ok: bool | None, msg: str = '')
  def _testar_conexao(self)
  def _fim_teste(self)
  def _aplicar_teste(self, seq: int, ok: bool | None, msg: str, porta_ok: int | None = None)
  def _test_tcp(self, ip: str, port: int, seq: int)
  def _test_escuta(self, port: int, seq: int)
  def _testar_todas_portas(self)
  def _scan_todas(self, ip: str, port: int, seq: int)
  def _mostrar_scan(self, texto: str)
  def _test_serial(self, porta: str, baud: int, seq: int, stopbits: float = 2)
  def _refresh_ports(self)
  def _ok(self)
class AgenteApp
  def __init__(self, root: tk.Tk)
  def _build_ui(self)
  def _rotulo_conexao(self, porta: str | None = None) -> str
  def _refresh_info(self)
  def _placeholder_cliente(self)
  def _label_cliente(self, cli: dict) -> str
  def _cliente_selecionado(self) -> dict | None
  def _url_imagem_cliente(self, cli: dict) -> str  # Foto do Cadastro de Cliente no servidor de pesagem (nunca Chamados / nunca local
  def _mostrar_placeholder_img(self, texto: str)
  def carregar_clientes(self)
  def _aplicar_lista_clientes(self, clientes: list)
  def _headers_api(self, cfg: dict | None = None) -> dict
  def _sync_enviados_scroll(self, _evt=None)
  def _on_mousewheel_enviados(self, evt)
  def _bind_scroll_enviados(self, _evt=None)
  def _unbind_scroll_enviados(self, _evt=None)
  def _mostrar_enviados_vazio(self, texto: str | None = None)
  def _filtrar_pesados_hoje(self, leituras: list, hoje: str) -> list
  def _mesclar_pesado(self, item: dict)
  def carregar_enviados_dia(self)
  def _aplicar_enviados_dia(self, leituras: list)
  def _montar_linha_enviado(self, item: dict)
  def _pedir_excluir_enviado(self, lid, nome='', liquido=None)
  def _pedir_editar_pesado(self, item: dict)
  def _salvar_edicao_pesado(self, dados: dict)
  def _excluir_enviado(self, lid: int)
  def _modo_aguardando_peso(self)  # Visor zerado, à espera do próximo peso da balança.
  def _on_cliente_sel(self, _evt=None)
  def _baixar_imagem(self, url: str, seq: int)
  def _exibir_imagem_bytes(self, data: bytes)
  def _on_foto_resize(self, evt)
  def _render_foto(self, w: int | None = None, h: int | None = None)
  def _set_status(self, text: str)
  def _abrir_config(self)
  def _aplicar_config(self, cfg: dict)
  def _reconectar(self)
  def _start_reader(self)
  def _toggle_sim(self)
  def _test_server(self)
  def _sobre(self)
  def _poll_queue(self)
  def _set_botoes_envio(self, habilitado: bool)
  def _texto_detalhe_peso(self) -> str
  def _refresh_detalhe(self)
  def _aplicar_pesos(self, bruto, *, aceso: bool = True)
  def _selecionar_campo_tara(self, _evt=None)  # Ao clicar na tara, seleciona o valor para o operador substituir (não concatenar 
  def _tara_select_all(self)
  def _recalcular_apos_tara(self)  # Atualiza líquido/visor com a tara atual, sem mexer no campo enquanto se digita.
  def _preview_tara_digitada(self, _evt=None)
  def _aplicar_tara_fixa(self, silencioso=False)
  def _abrir_ja_pesado(self)
  def _update_tara(self, data: dict)
  def _update_peso(self, data: dict)
  def enviar_peso(self, silencioso: bool = False, pesos: dict | None = None, origem: str = 'agente', observacao: str | None = None)
  def _on_close(self)
def main()

# agente_pesagem/make_icon.py [python]
imports: pathlib.Path, PIL.Image
def _is_colorful(r: int, g: int, b: int, a: int) -> bool
def _emblem_crop(im: Image.Image) -> Image.Image  # Recorta o emblema circular à esquerda do logo (texto fica de fora).
def main() -> None

# agente_pesagem/pesagem_calc.py [python]
def parse_peso_digitado(texto) -> float | None  # Converte texto do operador (12,5 / 12.5 / 5 kg) em kg.
def calcular_pesos(bruto, tara=0.0) -> dict  # Líquido = bruto − tara. Tara fixa do operador; bruto vem da balança.
def pesos_ja_pesado(liquido, tara=0.0) -> dict  # Produto/roupa já pesado: o operador informa o líquido (tara opcional do recipien
def bruto_da_leitura(data: dict) -> float | None  # Peso na plataforma (bruto). Prefere peso_bruto; senão o peso ao vivo.

# app.py [python]
imports: flask.Flask, routes.main, routes_nutricao.nutricao, routes_pesagem.pesagem, routes_acesso.acesso, routes_logistica.logistica, routes_audit.auditoria, models.db, models.Usuario, db_config.SQLALCHEMY_DATABASE_URI, models_nutricao, models_pesagem, models_acesso, models_logistica, models_audit, os, socket, threading
def create_app()
def _nome_usuario_sistema()  # Nome do usuário logado no Windows/sistema operacional.
def _app_revision()  # Short git SHA for /nutricao/versao and console startup.
def _port_available(host: str, port: int) -> bool  # Return True if we can bind host:port (then immediately release it).
def _resolve_https_port(host: str, preferred: int, fallback: int = 8443) -> int
def run_http_and_https(app, host: str, http_port: int, https_port: int)  # Serve HTTP on the main thread and HTTPS in a background thread.
def ensure_usuarios_schema()  # Garante colunas senha_hash, usuario e token em usuarios.
def ensure_chamados_schema()  # Garante colunas do modelo Chamado (tecnico_id, equipamento, atendimento) e FK de
def ensure_equipamentos_schema()  # Garante tabela/colunas de equipamentos (patrimônio vinculado ao cliente).
def ensure_clientes_schema()  # Garante colunas do modelo Cliente ausentes no banco legado.
def ensure_pesagem_schema()  # Garante tabela pesagem_clientes e colunas de cliente nas leituras.
def ensure_logistica_schema()  # Garante tabelas do Sistema de Controle de Logística e coluna perm_logistica.
def ensure_setores_funcao_schema()  # Cria setores_funcao e semeia os padrões de chamados e nutrição.
def ensure_tecnicos_schema()  # Cria tabelas chamado_setores e chamado_tecnicos e semeia setores padrão.
def ensure_cameras_schema()  # Garante tabela chamado_cameras (cadastro de câmeras).
def ensure_portoes_schema()  # Garante tabela chamado_portoes (cadastro de portões).
def ensure_estoque_schema()  # Garante tabelas chamado_estoque e chamado_estoque_usos.
def ensure_whatsapp_chamado_schema()
def ensure_operacao_chamados_schema()  # Tabelas e sementes: mesas, SLA, contratos, mensagens, automações, pastas.

# audit_service.py [python]
imports: json, re, datetime.datetime, datetime.date, datetime.timedelta, typing.Any, typing.Optional, flask.Request, flask.Response, flask.g, flask.has_request_context, flask.request, flask.session, sqlalchemy.inspect, sqlalchemy.or_, models.db, models_audit.AuditLog
def ensure_audit_table()  # Garante tabela audit_logs (create_all não altera schemas existentes).
def _modulo_de_path(path: str) -> str
def _acao_de_metodo(method: str, path: str) -> str
def _entidade_de_path(path: str) -> str
def _entidade_id_de_path(path: str) -> str
def _sanitize(obj: Any, depth: int = 0) -> Any
def _payload_resumo(req: Request, max_len: int = 1800) -> str
def _usuario_atual()
def registrar_auditoria(
    acao: str,
    *,
    modulo: Optional[str] = None,
    entidade: Optional[str] = None,
    entidade_id: Optional[str] = None,
    detalhe: Any = None,
    metodo: Optional[str] = None,
    caminho: Optional[str] = None,
    status_http: Optional[int] = None,
    sucesso: bool = True,
    usuario_id: Optional[int] = None,
    usuario_nome: Optional[str] = None,
    ip: Optional[str] = None,
)  # Grava um evento de auditoria (uso explícito ou via hook).
def deve_auditar_request(req: Request) -> bool
def log_from_request(req: Request, resp: Response)  # Hook after_request: registra mutações HTTP.
def listar_logs(
    *,
    modulo: Optional[str] = None,
    usuario: Optional[str] = None,
    acao: Optional[str] = None,
    q: Optional[str] = None,
    data_de: Optional[date] = None,
    data_ate: Optional[date] = None,
    limit: int = 200,
    offset: int = 0,
    ordem: str = 'desc',
)
def register_audit_hooks(app)  # Registra after_request global no app Flask.

# check_db.py [python]
imports: app.create_app, models.db, models.Usuario

# config_demo.py [python]
imports: db_config
class Config

# controlid_client.py [python]
imports: base64, logging, os, socket, time, dataclasses.dataclass, datetime.datetime, pathlib.Path, threading.Lock, typing.Any, requests
class ControlIDError(Exception)
class DeviceCreds
  def cache_key(self) -> str
  def base(self) -> str
def creds_from_equipamento(eq) -> DeviceCreds  # Monta DeviceCreds a partir de AcessoEquipamento (ou dict compatível).
def _http_session(key: str) -> requests.Session
def _cached_token(key: str) -> str | None
def _save_token(key: str, token: str) -> None
def invalidate_session(creds: DeviceCreds) -> None
def _session_invalid(resp: requests.Response | None) -> bool
def login(creds: DeviceCreds, force: bool = False, timeout: float | None = None) -> dict[str, Any]  # POST /login.fcgi → {session, device_id?}.
def device_post(
    creds: DeviceCreds,
    endpoint: str,
    payload: dict | list | None = None,
    timeout: float | None = None,
    force_login: bool = False,
) -> requests.Response  # POST /{endpoint}?session=... com retry em sessão inválida.
def device_json(
    creds: DeviceCreds,
    endpoint: str,
    payload: dict | list | None = None,
    timeout: float | None = None,
) -> Any
def _tcp_probe(host: str, port: int, timeout: float) -> str  # Retorna open | timeout | closed.
def tcp_reachable(host: str, porta: int = 80, timeout: float = 4.0) -> dict[str, Any]  # Checagem leve estilo Sollus: TCP na porta HTTP; 443 só se 80 recusar rápido.
def probe(creds: DeviceCreds, timeout: float = 4.0, try_login: bool = True) -> dict[str, Any]  # Reachability estilo Sollus: TCP 80/443 = online.
def system_information(creds: DeviceCreds) -> dict[str, Any]
def puxar_device_id(creds: DeviceCreds) -> str  # Obtém device_id via login e/ou system_information.
def set_system_time(creds: DeviceCreds, when: datetime | None = None) -> dict[str, Any]  # POST /set_system_time.fcgi com day/month/year/hour/minute/second.
def parse_data_hora_ui(data_str: str, hora_str: str) -> datetime  # Aceita data DD/MM/YYYY ou YYYY-MM-DD e hora HH:MM[:SS].
def configurar_monitor(
    creds: DeviceCreds,
    hostname: str,
    porta: int | str = 80,
    path: str | None = None,
    request_timeout: str | None = None,
    alive_interval: str | None = None,
) -> dict[str, Any]  # Configura servidor de eventos (monitor) via set_configuration.fcgi.
def ativar_modo_online_opcional(creds: DeviceCreds, server_id: int | str | None = None) -> dict[str, Any]  # Tenta habilitar online_client (Enterprise). Best-effort — nem todo firmware exig
def reiniciar_conexao(
    creds: DeviceCreds,
    hostname: str | None = None,
    porta: int | str | None = None,
    path: str | None = None,
) -> dict[str, Any]  # Reloga no device e reaplica monitor (equivalente Sollus 'reiniciar conexão').
def _extract_rows(data: Any, obj: str) -> list[dict]
def load_objects(creds: DeviceCreds, payload: dict, timeout: float = 20.0) -> dict
def fetch_access_logs_page(
    creds: DeviceCreds,
    after_id: int = 0,
    limit: int = OFFLINE_PAGE_SIZE,
) -> tuple[list[dict], int]  # Busca página de access_logs (3 formatos de payload, como no Sollus).
def collect_access_logs(
    creds: DeviceCreds,
    after_id: int = 0,
    max_pages: int = OFFLINE_MAX_PAGES,
    page_size: int = OFFLINE_PAGE_SIZE,
) -> list[dict]  # Coleta várias páginas de access_logs a partir de after_id.
def parse_access_log_time(row: dict) -> datetime  # Converte epoch (local, como Sollus) ou ISO para datetime naive.
def map_access_event(event_code: int | None, user_id: int | None = None, event_name: str | None = None) -> tuple[str, str]  # Retorna (status, event_type) alinhado ao mapeamento Sollus.
def create_objects(creds: DeviceCreds, object_name: str, values: list[dict]) -> dict
def destroy_objects(creds: DeviceCreds, object_name: str, where: dict) -> dict
def modify_objects(creds: DeviceCreds, object_name: str, values: dict, where: dict) -> dict
def load_users_by_registration(creds: DeviceCreds, registration: str) -> list[dict]
def upsert_user(
    creds: DeviceCreds,
    *,
    user_id: int | None,
    name: str,
    registration: str,
) -> dict[str, Any]  # Cria ou atualiza usuário no device. Prefere id explícito (= matrícula numérica).
def _card_api_value(card: str) -> int | str  # Converte cartão legível para valor API (int se numérico).
def upsert_card(creds: DeviceCreds, user_id: int, card: str) -> dict[str, Any]
def set_user_image(
    creds: DeviceCreds,
    user_id: int,
    image_bytes: bytes,
    timestamp: int | None = None,
) -> dict[str, Any]  # POST /user_set_image.fcgi com JPEG em base64.
def load_image_bytes(foto: str | None, static_root: Path | None = None) -> bytes | None  # Carrega bytes de foto a partir de data-URL, path web ou arquivo local.
def execute_door(creds: DeviceCreds, door: int = 1, actions: list[dict] | None = None) -> dict  # Abre porta via execute_actions.fcgi (door ou sec_box).
def list_device_users(creds: DeviceCreds, limit: int = 200) -> list[dict]

# create_db.py [python]
imports: pymysql, db_config.MYSQL_HOST, db_config.MYSQL_PORT, db_config.MYSQL_USER, db_config.MYSQL_PASSWORD, db_config.MYSQL_DATABASE

# db_config.py [python]
imports: os

# docs/nutricao/_gerar_apresentacao_diretoria.py [python]
imports: pathlib.Path, pptx.Presentation, pptx.dml.color.RGBColor, pptx.enum.shapes.MSO_SHAPE, pptx.enum.text.PP_ALIGN, pptx.enum.text.MSO_ANCHOR, pptx.util.Inches, pptx.util.Pt, pptx.util.Emu
def _set_run(run, text, size=18, bold=False, color=INK, font="Calibri")
def _add_rect(slide, left, top, width, height, fill)
def _add_accent_bar(slide)
def _add_footer(slide, page, total=13)
def _title_block(slide, title, subtitle=None)
def _bullets(slide, items, left=0.55, top=1.75, width=12.0, size=16)
def _two_columns(slide, left_items, right_items, top=1.85)
def cover(prs)
def slide_objetivo(prs)
def slide_visao(prs)
def slide_mapa(prs)
def slide_cadastros(prs)
def slide_precos(prs)
def slide_estoque(prs)
def slide_nutrientes(prs)
def slide_rastreio(prs)
def slide_seguranca(prs)
def slide_beneficios(prs)
def slide_roadmap(prs)
def slide_encerramento(prs)
def main()

# email_service.py [python]
imports: smtplib, email.message.EmailMessage, db_config.MAIL_FROM, db_config.MAIL_PASSWORD, db_config.MAIL_PORT, db_config.MAIL_SERVER, db_config.MAIL_USERNAME, db_config.MAIL_USE_TLS
def _config_env()
def obter_config_smtp()  # Usa a configuração salva no banco; se faltar, cai no ambiente / db_config.
def smtp_configurado() -> bool
def enviar_email(destinatario: str, assunto: str, texto: str, html: str | None = None) -> None
def _esc(valor) -> str
def montar_email_visualizacao_ticket(chamado, opener, url_detalhe: str)  # HTML estilo Tiflux: VISUALIZAÇÃO DE TICKET, para o usuário que abriu.
def enviar_visualizacao_ticket(chamado, opener, url_detalhe: str) -> None
def enviar_redefinicao_senha(destinatario: str, nome: str, link: str) -> None
def enviar_termo_responsabilidade(destinatario: str, nome: str, equipamento: str, link: str) -> None

# equipamento_service.py [python]
imports: base64, json, logging, os, random, re, secrets, string, threading, time, calendar.monthrange, datetime.date, datetime.datetime, datetime.timedelta, pathlib.Path, models.FREQUENCIAS_PREVENTIVA_KEYS, models.STATUS_FECHADOS, models.Chamado, models.ChamadoSetor, models.Equipamento, models.EquipamentoPreventiva, models.EquipamentoTermo, models.Usuario, models.aplicar_automacoes, models.db, models.mesa_padrao, models.now_brasilia
def add_months(d, months)
def avancar_data(d, frequencia)
def ocorrencias_preventiva(prev, ano=None, limite=None, max_itens=36)
def _retroceder_data(d, frequencia)
def _numero_os()
def _tecnico_preventiva(prev)
def _setor_tecnico_id(eq)
def _preventiva_aberta(eq_id)
def abrir_chamado_preventiva(eq, prev, hoje=None)  # Abre OS de preventiva se vencida e não houver ticket aberto do mesmo tipo.
def processar_preventivas(hoje=None)
def salvar_preventiva(eq, data, usuario)
def novo_token_termo()
def acessorios_from_payload(raw)
def snapshot_equipamento(eq)
def criar_ou_reenviar_termo(eq, data, usuario, link_builder)
def salvar_assinatura_png(token, data_url)
def assinar_termo(termo, payload)
def termo_para_api(termo, link=None)
def _scheduler_loop(app)
def start_preventiva_background(app)

# generate_certs.py [python]
imports: datetime, ipaddress, os, subprocess, sys, pathlib.Path
def generate_self_signed_certs(
    cert_dir: str | os.PathLike = 'certs',
    *,
    days: int = 825,
    force: bool = False,
) -> tuple[str, str]
def public_cert_pem_path(cert_dir: str | os.PathLike = 'certs') -> Path  # Caminho do certificado público (nunca a chave privada).
def load_public_cert_der(cert_dir: str | os.PathLike = 'certs') -> bytes  # Lê cert.pem e devolve DER (.cer) — só a parte pública.
def trust_cert_windows(cert_path: str | os.PathLike) -> None  # Importa cert.pem no Current User Trusted Root (Chrome/Edge).

# import_fdc_nutrientes.py [python]
imports: argparse, sys, app.create_app, nutricao_fdc_import.DEFAULT_TABELA_NOME, nutricao_fdc_import.import_fdc_foundation_foods
def main(argv=None)

# import_taco_nutrientes.py [python]
imports: argparse, sys, app.create_app, nutricao_taco_import.DEFAULT_TABELA_NOME, nutricao_taco_import.import_taco_table
def main(argv=None)

# init_db.py [python]
imports: app.create_app, models.db

# logistica_service.py [python]
imports: json, re, urllib, collections.defaultdict, datetime.date, datetime.datetime, datetime.timedelta, models_logistica.LogisticaEntrega, models_logistica.LogisticaLancamento, models_logistica.LogisticaManutencao, models_logistica.LogisticaOciosidade, models_logistica.LogisticaRota, models_logistica.LogisticaVeiculo
def totais_folha(folha)
def _parse_date(value)
def periodo_padrao(args)
def custo_operacional(data_de=None, data_ate=None, placa=None)  # Totais de custo, km e custo/km no período (fórmula do painel original).
def _chave_categoria(categoria)
def _eixos_periodo(data_de, data_ate)  # Lista de (chave, rótulo) cobrindo o intervalo filtrado.
def _bucket_data(valor, granularidade)
def evolucao_custos(data_de=None, data_ate=None, placa=None)  # Série temporal e composição dos custos respeitando o filtro da tela.
def custo_por_veiculo(data_de=None, data_ate=None, placa=None)
def alertas_dashboard(mes=None)
def _nominatim_search(params)
def _extrair_cep(texto)
def _limpar_endereco(texto)
def _coords_de_nominatim(rows, fallback_name)
def _nominatim_hit(fetch, q)
def _consultar_cep(cep)
def geocodificar_endereco(endereco, search=None, cep_lookup=None)  # Resolve endereço em lat/lng via Nominatim, com fallback pelo CEP.
def seed_logistica()  # Módulo começa vazio: o cadastro operacional fica nas telas.
def _haversine_km(lat1, lng1, lat2, lng2, ndigits=1)
def _osrm_fetch(url)
def polyline_e_incompleta(coords)
def km_da_polyline(coords)
def _polyline_de_osrm(payload, fallback)
def rotear_coordenadas(lat1, lng1, lat2, lng2, fetch=None, endpoints=None)  # Traça rota de carro pelas rodovias mais usadas (OSRM). Retorna polyline [[lat,ln
def garantir_coordenadas_ponto(ponto, geocode=None)
def polyline_entre_pontos(origem, destino, geocode=None, route_fetch=None)  # Garante coords dos pontos e devolve traçado pelas vias.
def retracar_rotas_incompletas(pontos=None, geocode=None, route_fetch=None, limit=20)  # Refaz o traçado de rotas que ficaram como linha reta (sem vias).
def sincronizar_pontos_clientes(geocode=None, limit_geo=10)  # Cria pontos de entrega a partir do cadastro de clientes (nome + endereço).

# migrations/002_linux_compat_columns.py [python]
def run(engine)

# migrations/003_create_nutricao_tables.py [python]
def run(engine)

# migrations/004_create_pesagem_and_expand_nutricao.py [python]
def run(engine)

# migrations/005_nutricao_precos_auditoria.py [python]
def run(engine)

# migrations/006_pesagem_clientes.py [python]
def run(engine)

# migrations/007_import_taco.py [python]
def run(engine)

# migrations/007_setores_funcao.py [python]
def run(engine)

# migrations/008_chamados_atendimento_linux.py [python]
def run(engine)

# migrations/008_pesagem_tara.py [python]
def run(engine)

# migrations/009_mapa_multi_dieta.py [python]
def run(engine)

# migrations/009_tiflux_sla_tecnicos.py [python]
def run(engine)

# migrations/010_pesagem_whatsapp.py [python]
def run(engine)

# migrations/010_ramais_equipamentos_local.py [python]
def run(engine)

# migrations/011_cameras_portoes.py [python]
def run(engine)

# migrations/011_nutricao_grupos_categorias.py [python]
def run(engine)

# migrations/012_estoque_nutricao_tenant.py [python]
def run(engine)

# migrations/012_whatsapp_chamado.py [python]
def run(engine)

# migrations/013_create_logistica.py [python]
def run(engine)

# migrations/013_marca_nutricao_refeicoes.py [python]
def run(engine)

# migrations/014_faturamento_nutricao_linux.py [python]
def run(engine)

# models.py [python]
imports: flask_sqlalchemy.SQLAlchemy, datetime.date, datetime.datetime, datetime.timedelta, decimal.Decimal, decimal.InvalidOperation
def now_brasilia()  # Horário atual em Brasília (naive), para gravar/exibir consistente no app.
def to_brasilia_naive(dt)  # Normaliza datetime para Brasília naive (sem tzinfo).
def fmt_brasilia(dt, fmt='%d/%m/%Y %H:%M')  # Formata datetime em horário de Brasília; string vazia se ausente.
def status_fechado(status)  # Atendido (técnico finalizou) e Concluído são estados encerrados.
def _fold_setor(valor)
def setores_padrao(tipo)
def listar_setores(tipo)  # Catálogo do dropdown: padrões sempre primeiro, depois extras cadastrados.
def listar_setores_detalhe(tipo)  # Lista setores cadastrados no banco (id, nome, padrao) para a grade.
def normalizar_setor(tipo, valor)  # Casa com o catálogo do tipo (padrões + extras), ignorando acento/caixa.
def normalizar_setor_chamado(valor)  # Normaliza o setor de encaminhamento (padrões + extras de chamados).
def adicionar_setor(tipo, nome)  # Inclui uma função/setor extra no catálogo do tipo. Não aceita vazio nem duplicat
def atualizar_setor(setor_id, nome)  # Renomeia um setor cadastrado (mesmo tipo, sem duplicar).
def excluir_setor(setor_id)  # Remove setor extra. Setores padrão do sistema não podem ser excluídos.
class Cliente(db.Model)
  def __repr__(self)
  def to_dict(self)
class Chamado(db.Model)
  def __repr__(self)
  def to_dict(self)
class Usuario(db.Model)
  def __repr__(self)
  def tem_sistema(self, sistema)
  def tem_menu(self, sistema, menu_key)
  def menus_liberados(self, sistema)
  def pode_gerenciar_acessos(self)  # Compat: Acessos no portal (menu superior) ou legado perm_acesso / master.
  def pode_opcao_portal(self, menu_key)  # Itens do menu Opções (canto superior direito) na home.
class SetorFuncao(db.Model)
class ChamadoSetor(db.Model)
  def __repr__(self)
class ChamadoTecnico(db.Model)
  def funcao_label(self)
  def __repr__(self)
class PermissaoMenu(db.Model)
class ConfiguracaoEmail(db.Model)
class RecursoGrupo(db.Model)
def grupo_recurso_padrao(cliente_id)
class Equipamento(db.Model)
  def __repr__(self)
  def equipamento(self)  # Compat: o banco usa nome_equipamento; alguns DBs não têm a coluna legado.
  def equipamento(self, value)
  def to_dict(self)
  def termo_atual(self)
  def _dict_preventiva_termo(self)
class EquipamentoPreventiva(db.Model)
class EquipamentoTermo(db.Model)
  def acessorios(self)
class ChamadoAtendimento(db.Model)
class ChamadoEncaminhamento(db.Model)
class ChamadoFoto(db.Model)
class ChamadoConhecimento(db.Model)
class MesaServico(db.Model)
class SlaPrioridade(db.Model)
class Contrato(db.Model)
class ChamadoMensagem(db.Model)
class ChamadoAutomacao(db.Model)
class WhatsAppChamadoConfig(db.Model)
class WhatsAppChamadoUsuario(db.Model)
  def completo(self)
  def to_dict(self)
class WhatsAppChamadoLog(db.Model)
class ChamadoRamal(db.Model)
  def __repr__(self)
  def to_dict(self)
class ChamadoCamera(db.Model)
  def __repr__(self)
  def to_dict(self)
class ChamadoPortao(db.Model)
  def __repr__(self)
  def to_dict(self)
class ChamadoEstoque(db.Model)
  def __repr__(self)
  def to_dict(self)
class ChamadoEstoqueUso(db.Model)
  def __repr__(self)
  def to_saida_dict(self)
class ConhecimentoPasta(db.Model)
def normalizar_prioridade(valor)
def sla_horas_prioridade(prioridade)
def sla_horas_tipo_contrato(tipo)
def contrato_vigente(cliente_id, ref_date=None)
def sla_horas_contrato(contrato)
def sla_do_chamado(chamado)
def _bucket_sla(dt, hoje, agora, fechado)
def mesas_ativas()
def mesa_padrao()
def resolver_mesa_id(raw)
def parse_valor_faturamento(raw)
def aplicar_automacoes(chamado, evento, usuario, status_anterior=None)  # evento: 'criar' | 'status'. Anexa nota na timeline e/ou muda a mesa.

# models_acesso.py [python]
imports: json, datetime.datetime, datetime.date, datetime.time, models.db
class AcessoEmpresa(db.Model)
  def to_dict(self)
class AcessoClassificacao(db.Model)
  def to_dict(self)
class AcessoDepartamento(db.Model)
  def to_dict(self)
class AcessoSetor(db.Model)
  def to_dict(self)
class AcessoCentroCusto(db.Model)
  def to_dict(self)
class AcessoLocal(db.Model)
  def to_dict(self)
class AcessoGrupo(db.Model)
  def to_dict(self)
class AcessoHorario(db.Model)
  def to_dict(self)
def normalizar_tipo_equipamento(value, default='catraca')
class AcessoEquipamento(db.Model)
  def tipo_norm(self)
  def to_dict(self, incluir_senha=False)
class AcessoPessoa(db.Model)
  def sync_ativo_status(self)
  def to_dict(self)
class AcessoTipoDocumento(db.Model)
  def to_dict(self, vinculos=None)
class AcessoPessoaDocumento(db.Model)
  def is_valido_efetivo(self)
  def to_dict(self)
class AcessoVisitante(db.Model)
  def to_dict(self)
class AcessoAmbiente(db.Model)
  def publico_list(self)
  def to_dict(self, detalhe=False)
class AcessoAmbienteEquipamento(db.Model)
  def to_dict(self)
class AcessoUsuarioPermissao(db.Model)
  def to_dict(self)
class AcessoPerfilPermissao(db.Model)
  def to_dict(self)
class AcessoBackupLog(db.Model)
  def to_dict(self)
class AcessoBackupConfig(db.Model)
  def to_dict(self)
class AcessoRefeicao(db.Model)
  def to_dict(self)
class AcessoEvento(db.Model)
  def status_exibicao(self)
  def to_dict(self)
class AcessoControleAdicional(db.Model)
  def expirado(self, ref=None)
  def to_dict(self)
class AcessoGrupoRefeicao(db.Model)
  def to_dict(self, detalhe=False)
class AcessoItemRefeicao(db.Model)
  def to_dict(self)
class AcessoVinculoRefeicao(db.Model)
  def to_dict(self)
class AcessoEstacionamento(db.Model)
  def to_dict(self, detalhe=False)
class AcessoEstacionamentoEquipamento(db.Model)
  def to_dict(self)
class AcessoEstacionamentoPermissao(db.Model)
  def to_dict(self)
def _nome_escala_valido(valor)
def dias_uteis_mes(data_ref=None)  # Dias 1–31 do mês da data_ref que não caem em sábado nem domingo.
def dias_padrao_escala(tipo, data_ref=None)  # Dias do mês (1–31) ativos segundo o tipo, antes de overrides.
def normalizar_tipo_escala(value)
def parse_dias_mes(raw, tipo='diaria', data_ref=None)  # Interpreta JSON/lista de dias ativos; fallback no padrão do tipo.
class AcessoEscala(db.Model)
  def normalize_tipo(self)
  def sync_tipo(self)  # Mantido por compatibilidade; tipo agora é par/ímpar/diária.
  def get_dias_ativos(self)
  def set_dias_ativos(self, days)
  def cobre_data(self, dia)  # True se o dia do calendário (date/datetime) está ativo nesta escala.
  def tipo_label(self)
  def display_nome(self)
  def to_dict(self, incluir_pessoas=False, qtd_pessoas=None)
class AcessoEscalaPessoa(db.Model)
  def to_dict(self)
class AcessoVeiculo(db.Model)
  def to_dict(self)
class AcessoVeiculoEvento(db.Model)
  def to_dict(self)
class AcessoImpressora(db.Model)
  def to_dict(self)

# models_audit.py [python]
imports: datetime.datetime, models.db
class AuditLog(db.Model)
  def to_dict(self)

# models_logistica.py [python]
imports: json, datetime.datetime, models.db
class LogisticaVeiculo(db.Model)
  def to_dict(self)
class LogisticaLancamento(db.Model)
  def to_dict(self)
class LogisticaManutencao(db.Model)
  def to_dict(self)
class LogisticaRevisao(db.Model)
  def to_dict(self)
class LogisticaRota(db.Model)
  def polyline_coords(self)
  def to_dict(self)
class LogisticaEntrega(db.Model)
  def to_dict(self)
class LogisticaColaborador(db.Model)
  def to_dict(self)
class LogisticaFolha(db.Model)
  def to_dict(self)
class LogisticaFolga(db.Model)
  def to_dict(self)
class LogisticaOciosidade(db.Model)
  def to_dict(self)
class LogisticaChecklist(db.Model)
  def to_dict(self)
class LogisticaHigiene(db.Model)
  def to_dict(self)
class LogisticaColeta(db.Model)
  def to_dict(self)

# models_nutricao.py [python]
imports: datetime.datetime, datetime.date, models.db, models.now_brasilia, models.fmt_brasilia
class NutClinica(db.Model)
  def to_dict(self, include_enfermarias=False)
class NutGrupoClinica(db.Model)
  def to_dict(self, include_clinicas=False)
class NutEnfermaria(db.Model)
  def to_dict(self, include_leitos=False, num_leitos=None)
class NutLeito(db.Model)
  def to_dict(self)
class NutDieta(db.Model)
  def to_dict(self)
class NutDietaExcluida(db.Model)
class NutGrupoDieta(db.Model)
  def to_dict(self)
class NutCategoriaDieta(db.Model)
  def to_dict(self)
class NutPaciente(db.Model)
  def idade(self, ref=None)
  def imc(self)
  def to_dict(self)
class NutMapaRefeicao(db.Model)
  def get_substituicoes(self)
  def set_substituicoes(self, data)
  def to_dict(self)
class NutCardapio(db.Model)
  def get_itens(self)
  def set_itens(self, data)
  def to_dict(self)
class NutTabelaNutrientes(db.Model)
  def to_dict(self)
class NutAlimento(db.Model)
  def to_dict(self, include_nutrientes=True)
class NutAlimentoNutriente(db.Model)
  def to_dict(self)
class NutPratoLiquido(db.Model)
  def to_dict(self)
class NutEstoqueLocal(db.Model)
  def to_dict(self)
class NutUnidadeMedida(db.Model)
  def to_dict(self)
class NutGrupoProduto(db.Model)
  def to_dict(self)
class NutProduto(db.Model)
  def to_dict(self)
class NutFornecedor(db.Model)
  def to_dict(self)
class NutEtiqueta(db.Model)
  def to_dict(self, include_campos=True)
class NutEtiquetaCampo(db.Model)
  def to_dict(self)
class NutPrecoRefeicao(db.Model)
  def to_dict(self)
class NutTipoRefeicao(db.Model)
  def to_dict(self)
class NutPrecoDietaTipo(db.Model)
  def to_dict(self)
class NutRefeicaoAcompanhante(db.Model)
  def to_dict(self, exclusao_bloqueada=None)
class NutRefeicaoFuncionario(db.Model)
  def to_dict(self)

# models_pesagem.py [python]
imports: datetime.datetime, models.db
class PesagemBalanca(db.Model)
  def to_dict(self)
class PesagemCliente(db.Model)
  def to_dict(self, imagem_url='')
class PesagemLeitura(db.Model)
  def to_dict(self)
class PesagemWhatsAppDestino(db.Model)
  def to_dict(self)
class PesagemWhatsAppEnvio(db.Model)
  def to_dict(self)

# nutricao_fdc_import.py [python]
imports: json, zipfile, datetime.datetime, pathlib.Path, typing.BinaryIO, typing.Dict, typing.List, typing.Optional, typing.Tuple, typing.Union, models.db, models_nutricao.NutAlimento, models_nutricao.NutAlimentoNutriente, models_nutricao.NutTabelaNutrientes
def _norm_unit(unit: Optional[str]) -> str
def _nutrient_label(nutrient: dict) -> Tuple[str, str]
def _ref_consumo(food: dict) -> str
def _macros_from_nutrients(amounts: Dict[int, float]) -> dict
def _extract_nutrient_rows(food: dict) -> Tuple[List[dict], Dict[int, float]]
def load_foundation_foods(source: Union[str, Path, BinaryIO]) -> List[dict]  # Load FoundationFoods array from .json path, .zip path, or file-like object.
def _read_json_payload(source: Union[str, Path, BinaryIO]) -> dict
def _parse_bytes(raw: bytes, name_hint: Optional[str] = None) -> dict
def import_fdc_foundation_foods(
    source: Union[str, Path, BinaryIO],
    *,
    tabela_nome: str = DEFAULT_TABELA_NOME,
    set_official: bool = True,
    batch_size: int = 25,
    deactivate_missing: bool = True,
) -> dict  # Upsert Foundation Foods into NutTabelaNutrientes / NutAlimento / NutAlimentoNutr

# nutricao_seed_cardapios.py [python]

# nutricao_seed_dietas.py [python]
def _alm(v)
def precos_dict_da_tupla(precos_6)  # Converte tupla de 6 floats em dict nome_tipo → valor.

# nutricao_seed_enfermarias.py [python]

# nutricao_seed_etiquetas.py [python]

# nutricao_seed_fornecedores.py [python]

# nutricao_seed_lactario.py [python]

# nutricao_seed_nutricional.py [python]

# nutricao_seed_pratos_liquidos.py [python]

# nutricao_seed_produtos.py [python]

# nutricao_service.py [python]
imports: datetime.date, datetime.datetime, datetime.timedelta, re, unicodedata, models.db, models.now_brasilia, models.fmt_brasilia, models_nutricao.NutClinica, models_nutricao.NutGrupoClinica, models_nutricao.NutEnfermaria, models_nutricao.NutLeito, models_nutricao.NutDieta, models_nutricao.NutDietaExcluida, models_nutricao.NutGrupoDieta, models_nutricao.NutCategoriaDieta, models_nutricao.NutPaciente, models_nutricao.NutMapaRefeicao, models_nutricao.NutCardapio, models_nutricao.NutTabelaNutrientes, models_nutricao.NutAlimento, models_nutricao.NutAlimentoNutriente, models_nutricao.NutPratoLiquido, models_nutricao.NutEstoqueLocal, models_nutricao.NutUnidadeMedida, models_nutricao.NutGrupoProduto, models_nutricao.NutProduto, models_nutricao.NutFornecedor, models_nutricao.NutEtiqueta, models_nutricao.NutEtiquetaCampo, models_nutricao.NutPrecoRefeicao, models_nutricao.NutTipoRefeicao, models_nutricao.NutPrecoDietaTipo, models_nutricao.NutRefeicaoAcompanhante, models_nutricao.NutRefeicaoFuncionario
def normalizar_sexo(valor)  # Aceita M/F/O ou os nomes masculino/feminino/outros.
def sexo_label(valor)
def _q(model, cliente_id=None)  # Query scoped to current nutrition client when applicable.
def _parse_date(value)
def list_dietas(somente_ativas=False)
def list_grupos_dieta(somente_ativos=False)
def list_categorias_dieta(somente_ativos=False)
def slug_categoria_dieta(nome)
def codigo_categoria_unico(base, exclude_id=None)
def _seed_categorias_dieta()  # Garante categorias padrão + valores já usados em NutDieta.categoria.
def _seed_grupos_dieta()  # Garante grupos de dieta padrão + valores já usados em NutDieta.grupo.
def list_tipos_refeicao(somente_ativos=False)
def list_clinicas(somente_ativas=False)
def list_grupos_clinica(somente_ativos=False, include_clinicas=False)
def nomes_clinicas_do_grupo(grupo_id=None, grupo_nome=None)  # Nomes (uppercase) das clínicas vinculadas a um grupo.
def expand_filtros_grupo_clinica(filtros)  # Converte ids/nomes de grupo em rótulos e conjunto de clínicas.
def list_enfermarias(somente_ativas=False)
def list_leitos(enfermaria_id=None, somente_ativos=False)
def _ensure_nutricao_columns(force=False)  # Adiciona colunas novas em tabelas já existentes (create_all não altera).
def _backfill_cardapio_dieta_id()  # Preenche dieta_id a partir do nome denormalizado (idempotente).
def _seed_enfermarias()
def list_cardapios(tipo=None, dieta_id=None)
def _ensure_dietas_excluidas_table()
def _norm_dieta_nome(nome)
def dieta_foi_excluida(nome, cliente_id=None)  # True se o nome foi removido do cadastro e o seed não deve recriar.
def marcar_dieta_excluida(nome, cliente_id=None)
def liberar_dieta_excluida(nome, cliente_id=None)  # Permite cadastrar de novo um nome que tinha sido excluído.
def excluir_dieta_e_cardapios(dieta)  # Remove a dieta do cadastro, cardápios e preços vinculados.
def tipo_refeicao_para_meal(valor)  # Aceita chave (almoco), sigla (ALM) ou nome (Almoço) e devolve a chave interna.
def list_tipos_refeicao_impressao()  # Tipos ativos do cadastro com chave interna para filtros de impressão.
def mapa_horas_limite()  # Retorna {meal_key: 'HH:MM'} a partir do cadastro de tipos de refeição.
def _agora_brasilia()
def refeicao_apos_hora_limite(meal, data_mapa, agora=None)  # True se, no dia do mapa (= hoje), já passou a hora limite da refeição.
def data_destino_lancamento(meal, data_mapa, agora=None)  # Data em que a alteração da refeição deve ser lançada/salva.
def info_lancamento_refeicoes(data_mapa, agora=None)  # Metadados de hora limite / destino por refeição (para UI e API).
def buscar_linha_mapa_dia(src, data_alvo)  # Localiza a linha do mesmo lançamento em data_alvo (não cria).
def obter_ou_criar_linha_mapa_dia(src, data_alvo, usuario=None)  # Localiza (ou cria a partir de src) a linha do paciente em data_alvo.
def _vals_prato(val)
def _chave_item_cardapio(key)
def _norm_txt(s)
def _dieta_match_score(paciente_dieta, cardapio_dieta)
def pratos_from_itens(itens, tipo=None)  # Extrai lista ordenada de pratos a partir do JSON de itens do cardápio.
def normalize_substituicoes(raw)  # Normaliza JSON de substituições para as 6 refeições.
def cardapio_personalizado(pratos_padrao, pares)  # Aplica Remover/Adicionar sobre o cardápio padrão.
def find_cardapio_for_meal(dieta, meal, data_ref=None, dieta_id=None)  # Melhor NutCardapio para dieta + horário (opcionalmente dia do mês).
def get_mapa_substituicoes(mapa_row)  # Monta payload completo do diálogo Substituições para uma linha do mapa.
def save_mapa_substituicoes(mapa_row, payload, usuario=None)  # Persiste substituições respeitando hora limite (adia para o dia seguinte).
def importar_substituicoes_anteriores(mapa_row, meal=None, so_justificativa=False)  # Copia pares/justificativa do mapa anterior do mesmo paciente.
def listar_justificativas_anteriores(mapa_row, meal=None, limit=40)  # Lista justificativas já usadas em mapas anteriores do mesmo paciente.
def _cardapio_seed_signature(item)
def _seed_cardapios()
def list_tabelas_nutrientes(somente_ativas=True)
def list_alimentos(tabela_id=None, somente_ativas=True, include_nutrientes=True)
def import_tabela_fdc(source, **kwargs)  # Proxy para import USDA FDC Foundation Foods (zip/json path ou file-like).
def _seed_nutricional()
def list_pratos_liquidos(somente_ativos=True)
def _seed_pratos_liquidos()
def list_estoques(somente_ativos=True)
def list_unidades(somente_ativas=True)
def list_grupos_produto(somente_ativos=True)
def list_produtos(estoque_id=None, grupo_id=None, somente_ativos=True)
def nomes_grupo_tema_cardapio(tema, variantes_prato=False)  # Nomes de grupo equivalentes ao tema do campo (BEBIDA ≈ BEBIDAS, PRATO 1–7 ≈ PRAT
def estoque_padrao_produto()
def garantir_grupo_produto_tema(tema)
def _codigo_produto_unico(estoque_id, nome)
def list_produtos_tema_cardapio(tema)
def list_produtos_temas_cardapio(temas)
def criar_produto_tema_cardapio(nome, tema)
def _seed_produtos()
def list_fornecedores(somente_ativos=True)
def _seed_fornecedores()
def list_etiquetas(somente_ativas=False)
def _seed_etiquetas()
def _p3(v)  # Mesmo preço nas três colunas.
def normalizar_hora_limite(valor)  # Aceita HH:MM / HHMM / time; devolve 'HH:MM' ou ''.
def list_precos_refeicoes(somente_ativos=True)  # Legado: catálogo plano (mantido inativo; não é a tela principal).
def list_precos_dieta_tipo(somente_ativas=True, somente_tipos_ativos=True)  # Lista preços dieta × tipo (linhas planas).
def matriz_precos_dieta_tipo(somente_ativas=True, somente_tipos_ativos=True)  # Grade Dieta × Tipo de Refeição para a tela de preços.
def _seed_tipos_refeicao()
def _normalize_precos_map(precos_map=None)  # Aceita dict por nome/sigla de tipo → float; devolve dict nome_tipo UPPER → float
def _payer_fields(payers=None)  # Normaliza lista de colunas: empresa / paciente / acompanhante.
def ensure_precos_para_dieta(
    dieta_id,
    aplicar_default=True,
    precos_map=None,
    forcar=False,
    payers=None,
    tipos_ids=None,
)  # Cria/atualiza células dieta × tipos ativos.
def _upsert_dieta_catalogo(nome, cat, grupo, ativo, precos, payers=None)  # Upsert uma dieta do catálogo e força preços nas colunas indicadas.
def _seed_catalogo_dietas_foto()  # Upsert dietas + preços do catálogo da foto (idempotente por nome).
def _seed_catalogo_lactario()  # Upsert catálogo LACTÁRIO (OCR).
def _seed_precos_dieta_tipo()  # Garante matriz dieta × tipo; catálogo da foto + LACTÁRIO com upsert forçado.
def _seed_precos_refeicoes()  # Desativa o catálogo plano antigo (enterais/itens) da tela de preços.
def seed_nutricao(force=False)  # Garante cadastros básicos de clínicas e dietas.
def paciente_from_payload(d, paciente=None)
def normalizar_tipo_lancamento(valor, default='principal')
def rotulo_tipo_lancamento(tipo)
def ordem_tipo_lancamento(tipo)
def resolver_dieta_mapa(dieta_id=None, dieta_nome=None)  # Resolve NutDieta ativa por id ou nome (sem criar cadastro).
def garantir_grupo_lancamento(row)  # Garante lancamento_grupo_id = id na primeira persistência do lançamento.
def _chave_grupo_lancamento(row)
def mapa_from_paciente(paciente, data_ref=None, flags=None, extras=None, usuario=None)
def criar_lancamento_dieta_desde_linha(src, flags=None, extras=None, usuario=None, tipo_lancamento='adicional')  # Novo lançamento de dieta no mesmo paciente/leito, sem duplicar o cadastro.
def _chave_linha_mapa(row)  # Chave de identidade para linhas sem paciente_id.
def _clonar_linha_mapa(src, data_ref, usuario=None)  # Copia snapshot operacional de uma linha ativa para outra data.
def _linha_com_baixa(src)  # Baixa real no mapa: só Excluir com motivo preenchido.
def _linha_mapa_ativa_para_copia(src)  # Elegível para persistir no dia seguinte (não deu baixa com motivo).
def _sanear_baixas_incompletas(data_ref)  # Reativa linhas inativas sem motivo (baixa incompleta / bug antigo).
def _upsert_linha_no_dia(src, data_ref, by_grupo, by_chave)  # Garante o lançamento de src em data_ref (cria ou reativa). Retorna 1 se alterou.
def _indices_linhas_mapa(existentes)
def _copiar_ativos_dia_anterior(data_ref)  # Merge: cada lançamento ativo (sem baixa com motivo) de data_ref-1 passa a existi
def _garantir_ausentes_desde_historico(data_ref)  # Copia do último estado anterior (sem baixa) quem ainda falta no dia alvo.
def garantir_mapa_do_dia(data_ref=None)  # Garante linhas do mapa em data_ref mesclando pacientes ainda sem baixa.
def marcar_alteracao_mapa(row, usuario=None)
def leito_ocupado_no_mapa(data_ref, clinica, enfermaria, leito, exclude_id=None, paciente_id=None)  # True se o leito já tem outro paciente ativo (mesmo paciente pode ter várias diet
def _chaves_ocupacao_leito(valor)  # Normaliza rótulos de leito para comparação (nº, nome, '1 — Janela').
def list_leitos_vagos(
    data_ref,
    clinica=None,
    enfermaria=None,
    enfermaria_id=None,
    exclude_mapa_id=None,
)  # Leitos cadastrados ativos da enfermaria que não estão ocupados no mapa do dia.
def _rotulo_valor_leito(leito)  # Rótulo de exibição e valor canônico de um leito cadastrado.
def montar_grade_leitos_mapa(data_ref, clinica_nome=None, clinica_nomes=None)  # Uma linha por leito cadastrado da clínica, com paciente do mapa se ocupado.
def _aplicar_baixa_linha(row, motivo, usuario=None, data_saida=None, hospital_transferencia=None)  # Aplica campos de baixa em uma linha do mapa (mantém registro histórico).
def resolver_partes_acompanhante(data_ref, fl_almoco=True, fl_jantar=True, agora=None)  # Divide lançamento A/J entre o dia pedido e o dia seguinte (hora limite).
def paciente_ativo_no_mapa(paciente_id)  # True se o paciente ainda tem linha ativa no mapa de refeições (qualquer dia).
def _chave_acompanhante(row)  # Identidade estável do lançamento (paciente + nome) para copiar entre dias.
def _clonar_acompanhante_no_dia(src, data_ref)  # Cria cópia ativa do acompanhante em data_ref (sem commit).
def garantir_acompanhantes_do_dia(data_ref=None, commit=True)  # Copia refeições de acompanhante para o dia (e preenche dias intermediários).
def baixar_acompanhantes_do_paciente(paciente_id, motivo=None, data_saida=None)  # Baixa (soft) todos os acompanhantes ativos do paciente (saída do mapa).
def registrar_saida_mapa(row, motivo, usuario=None, data_saida=None, hospital_transferencia=None)  # Baixa/soft-exit: mantém histórico na linha (não apaga) e impede persistência fut
def listar_avisos_alta_mapa(data_ref=None)  # Pacientes com alta anteriores à data do mapa, ainda presentes nas linhas ativas.
def aplicar_avisos_alta_mapa(mapa_ids_excluir, usuario=None)  # Desativa linhas do mapa marcadas para exclusão no aviso de alta (com histórico).
def totalizar_mapa_uma(data_ref, categoria='enteral', imprimir_por='clinica', filtro_id=None, filtro_nome=None, horarios=None, amostra=None)  # Agrega itens do mapa (enteral/suplementos/fórmula/LVE) para impressão U.M.A.
def _classificar_dieta_faturamento(linha, dieta_cat_map=None)  # Classifica linha do mapa em BÁSICAS / LÍQUIDAS / D.I. / ACOMP.
def _zero_matriz()
def _clinica_para_acompanhante(a, data_ref, clinica_por_paciente_dia=None)  # Resolve clínica do lançamento de acompanhante (mapa do dia ou cadastro do pacien
def _somar_acompanhantes_faturamento(blocos, totais_gerais, data_de, data_ate, clinica_por_paciente_dia)  # Inclui lançamentos de nut_refeicao_acompanhantes na categoria ACOMP.
def relatorio_faturamento(data_de, data_ate, tipo='espelho_1', por_grupo_clinica=False, sintetico=False)  # Gera espelho/totais de faturamento a partir do mapa + refeições acompanhante.
def _norm_nome_preco(s)
def _indexes_precos_faturamento()  # Índices para resolver preço de dieta×tipo, catálogo plano e produtos.
def _resolver_dieta_id(nome_item, idx)  # Resolve dieta com preço: match exato, senão nome-base mais adequado com preço.
def _tipo_id_para_ref(ref_key, idx)
def _preco_unitario_item(nome_item, ref_key, payer, idx)  # Resolve preço unitário: cadastro dieta×tipo → catálogo plano → preço médio produ
def _add_linha_fat_valor(linhas, *, data_ref, fonte, pessoa, clinica, item, ref_key, qtd, payer, idx)
def relatorio_faturamento_valores(data_de, data_ate)  # Faturamento com valores: mapa + acompanhante + funcionários × preços.
def _grupo_dieta_totalizacao(nome_dieta, dieta_cat_map=None)  # Agrupa dieta em BÁSICAS / LÍQUIDAS / D.I. / ACOMPANHANTES para subtotais.
def totalizacao_dietas(
    data_ref=None,
    totalizacao_para='clinicas',
    imprimir_por='clinica',
    filtros=None,
    horarios=None,
    metodo='mapa',
    imprimir_total_geral=True,
)  # Totaliza dietas do mapa por nome × horários (estilo relatório legado).
def totalizacao_dietas_refeicoes(
    data_ref=None,
    totalizacao_para='clinicas',
    imprimir_por='clinica',
    filtros=None,
    horarios=None,
    metodo='todas',
    imprimir_total_geral=True,
)  # Totaliza dietas do mapa + refeições de acompanhantes + funcionários.
def list_modelos_etiqueta_impressao()  # Modelos pré-configurados + personalizados (cadastro NutEtiqueta ou fallback).
def _geometria_etiqueta(base_id, extra=None)  # Medidas oficiais do modelo + overrides do cadastro (mm).
def _posicoes_etiquetas(geom, cols, rows)
def _resolver_modelo_etiqueta(modelo_id)
def _obs_linha_etiqueta(row)
def _fmt_dn(pac, idade=None)
def _fmt_leito(leito)
def _fmt_data_hora_etiqueta(row)  # Última alteração do mapa; se ausente, data de inclusão (dd/mm/yyyy HH:mm).
def gerar_impressao_etiquetas(
    data_ref=None,
    horario='desjejum',
    modo='mapa',
    imprimir_por='clinica',
    filtro_id=None,
    filtro_nome=None,
    ordenar='grupo_dieta_data',
    incluir_enfermaria=False,
    somente_alteradas=False,
    alteradas_desde=None,
    modelo_id='6080',
    seq_inicio=1,
)  # Gera etiquetas de dieta a partir do mapa de refeições do dia.
def _fmt_data_br(valor)
def _marca_x(valor)
def _marca_isolamento(row)  # Coluna I do mapa impresso: isolamento indicado em diagnóstico/obs.
def gerar_impressao_mapa(data_de=None, data_ate=None, clinica_nome=None)  # Relatório do mapa de produção: linhas do período com totais de dietas e refeiçõe
def gerar_impressao_mapa_distribuicao(
    data_de=None,
    data_ate=None,
    clinica_nomes=None,
    tipo_refeicao='almoco',
)  # Lista de distribuição: pacientes da refeição, agrupados por clínica.

# nutricao_taco_import.py [python]
imports: csv, io, datetime.datetime, pathlib.Path, models.db, models_nutricao.NutAlimento, models_nutricao.NutAlimentoNutriente, models_nutricao.NutTabelaNutrientes
def _num(val)
def _rnd(val, nd=2)
def _load_rows(source=None)
def import_taco_table(
    source=None,
    tabela_nome=DEFAULT_TABELA_NOME,
    set_official=False,
    batch_size=50,
    deactivate_missing=True,
)  # Upsert TACO em nut_tabelas_nutrientes / nut_alimentos (idempotente por número TA

# nutricao_tenant.py [python]
imports: flask.g, flask.has_request_context, flask.session
def ensure_cliente_hfb()  # Create/find HOSPITAL FEDERAL DE BONSUCESSO with nutricao enabled.
def ensure_nutricao_cliente_schema()  # Add cliente_id to nutrição tables, relax nome uniques, backfill HFB.
def usuario_sessao_obj()
def usuario_ve_todos_clientes(user)  # Master/admin or explicit cliente_todos flag → unscoped nutrition view.
def label_cliente_nutricao(user=None)  # Sidebar / portal label for nutrition client scope.
def resolve_nutricao_cliente(user=None)  # Return (user, cliente, cliente_id).
def current_cliente_id(default=None)
def current_cliente()
def write_cliente_id()  # Cliente id for inserts. Master/todos without link → HFB.
def apply_cliente_filter(query, model, cliente_id=None)  # Filter query by cliente_id when set. None = no filter (master/todos all).
def scoped_query(model, cliente_id=None)
def row_belongs_to_cliente(row, cliente_id=None)
def require_cliente_for_write()  # Return (ok, error_message, cliente_id_for_write).

# password_utils.py [python]
imports: hashlib, hmac, werkzeug
def generate_password_hash(password: str) -> str
def check_password_hash(pwhash: str, password: str) -> bool
def _check_scrypt(method: str, salt: str, hashval: str, password: str) -> bool

# permissions_sistemas.py [python]
imports: password_utils.generate_password_hash
def aplicar_permissoes_formulario(usuario, form)
def conceder_acesso_total(usuario)
def garantir_acesso_master()
def ensure_portal_opcoes_legado()  # Garante perm_portal + menus do menu superior para quem já tinha Acessos.

# recreate_db.py [python]
imports: app.create_app, models.db

# routes.py [python]
imports: flask.Blueprint, flask.Response, flask.render_template, flask.request, flask.jsonify, flask.redirect, flask.url_for, flask.flash, flask.session, functools.wraps, password_utils.generate_password_hash, password_utils.check_password_hash, models.db, models.Chamado, models.Usuario, models.Cliente, models.Equipamento, models.EquipamentoPreventiva, models.EquipamentoTermo, models.RecursoGrupo, models.ChamadoSetor, models.ChamadoTecnico, models.ConfiguracaoEmail, models.ChamadoAtendimento, models.ChamadoEncaminhamento, models.ChamadoFoto, models.ChamadoConhecimento, models.MesaServico, models.SlaPrioridade, models.Contrato, models.ChamadoMensagem, models.ChamadoAutomacao, models.ChamadoRamal, models.ChamadoCamera, models.ChamadoPortao, models.ChamadoEstoque, models.ChamadoEstoqueUso, models.PermissaoMenu, models.ConhecimentoPasta, models.SetorFuncao, models.WhatsAppChamadoUsuario, models.now_brasilia, models.fmt_brasilia, models.TIPO_FOTO_CONSERTO, models.TIPO_FOTO_ENCAMINHAMENTO, models.TIPO_HOP_ENCAMINHAR, models.TIPO_HOP_DEVOLVER, models.TIPO_HOP_PECA, models.SETOR_COMPRAS, models.STATUS_AGUARDAR_PECA, models.STATUS_ENCAMINHADO, models.STATUS_DEVOLVIDO, models.STATUS_ATENDIDO, models.STATUS_FECHADOS, models.TIPO_SETOR_CHAMADOS, models.TIPO_SETOR_NUTRICAO, models.TIPOS_CONTRATO, models.FUNCOES_TECNICO, models.FUNCOES_TECNICO_KEYS, models.CANAIS_MENSAGEM, models.CANAIS_ENVIO_LIVE, models.PASTA_CONHECIMENTO_PADRAO, models.status_fechado, models.normalizar_setor_chamado, models.normalizar_setor, models.listar_setores, models.listar_setores_detalhe, models.adicionar_setor, models.atualizar_setor, models.excluir_setor, models.sla_do_chamado, models._bucket_sla, models.mesas_ativas, models.mesa_padrao, models.resolver_mesa_id, models.aplicar_automacoes, models.parse_valor_faturamento, models.normalizar_prioridade, models.contrato_vigente, models.sla_horas_tipo_contrato, models.TICKET_PARADO_HORAS, models.TIPOS_RECURSO, models.FREQUENCIAS_PREVENTIVA, models.grupo_recurso_padrao
def _http_cert_download_url() -> str  # URL HTTP (sem TLS) para baixar o .cer — evita aviso SSL no instalador.
def _build_trust_installer_bat(cert_url: str) -> str  # Gera .bat que baixa o .cer público e importa no Current User\\Root.
def login_required(f)
def _checar_permissao_menu_chamados()
def _parse_data_compra(valor)
def _clientes_para_chamados()  # Clientes habilitados para Gestão de Chamados.
def _vincular_equipamento_chamado(chamado, form, cliente_id)  # Grava patrimônio, nome e FK a partir do cadastro de equipamentos.
def gerar_numero_chamado()  # Gera um número único para o chamado
def _setor_usuario(usuario)
def _tecnico_vinculado(usuario)  # Técnico ativo ligado ao acesso (usuario_id ou e-mail).
def _eh_tecnico_do_sistema(usuario)  # Usuário cadastrado em Técnicos (ou gestor) — vê a fila de tickets abertos.
def _registros_tecnico_usuario(usuario)  # Cadastros ativos em Técnicos ligados ao acesso (usuario_id ou e-mail).
def _recebe_campanha_ticket(usuario)  # Técnico, supervisor, gestor (cadastro) ou admin/master recebem o aviso de chamad
def _tecnico_com_email_vinculado(usuario)  # Técnico só vale para o cumprimento se o cadastro tiver e-mail.
def _primeiro_nome(usuario)  # Nome do técnico (se houver e-mail) ou o nome cadastrado em Acessos.
def _nome_tecnico_aviso(usuario)  # Nome completo do técnico para aviso de finalização.
def _filtro_chamados_usuario(user)  # Tickets visíveis: os que o usuário abriu, encaminhados ao setor e, para técnicos
def _estagio_do_status(status)
def _titulo_chamado(chamado)
def _url_detalhe_chamado(chamado_id)
def _fmt_dt(valor)
def _contar_sla_widgets(chamados_abertos)
def _avisar_abertura_ticket(chamado, opener)  # E-mail VISUALIZAÇÃO DE TICKET só para quem abriu. Não derruba a criação.
def montar_texto_finalizacao(chamado, tecnico_nome, hora, o_que_foi_feito)
def _canal_abertura_chamado(chamado)
def _contato_abertura_chamado(chamado, canal)
def _avisar_finalizacao_ticket(chamado, tecnico, o_que_foi_feito, enviar_wa=None, enviar_mail=None, agora=None)  # Avisa quem abriu, no mesmo canal da abertura. Não derruba a finalização.
def _ultima_movimentacao(chamado)
def _alertas_sla_e_parada(chamados_abertos)  # Alertas de SLA próximo/vencido e ticket parado — alimentam o sino.
def _status_inicial_novo()
def _query_chamados_usuario(user)
def _grupos_tickets(chamados)
def _eh_gestor(usuario)
def _ultimo_hop(chamado)
def _setor_origem_atual(chamado)
def _pode_devolver(chamado, usuario)  # Técnico do setor destino de um encaminhamento aberto (não de uma devolução).
def _setor_atuacao(usuario, chamado)
def _normalizar_email(email)  # E-mail normalizado para vínculo técnico ↔ acesso (trim + lower).
def _usuario_por_email(email)  # Busca Usuario (Acesso) pelo e-mail normalizado.
def _vincular_tecnicos_ao_usuario(usuario)  # Liga ChamadoTecnico ↔ Usuario pelo e-mail.
def _setores_tecnico_usuario(usuario)  # IDs de chamado_setores onde o usuário está cadastrado como técnico.
def _pendencias_chamados(usuario)  # Pendências do login: tickets abertos (técnicos), encaminhamentos e aguardando pe
def _tipo_foto_chamado(foto)
def _fotos_chamado_payload(chamado, tipo=None)
def _arquivos_request(*nomes)
def _salvar_fotos_chamado(chamado, atendimento, arquivos, tipo=TIPO_FOTO_CONSERTO)
def _chamado_atender_payload(chamado, usuario=None)
def instalar_certificado()  # Ajuda pública para confiar no certificado autoassinado (Windows).
def download_certificado_cer()  # Serve só o certificado público em DER (.cer). Nunca a chave privada.
def download_instalar_certificado_bat()  # Instalador Windows: baixa o .cer público e importa no Trusted Root do usuário.
def download_trust_local_cert_ps1()  # PowerShell one-liner-friendly: importa .cer ao lado ou baixa via HTTP.
def login()  # Página de login
def logout()  # Logout do usuário
def _gerar_link_redefinicao(usuario)
def _enviar_link_redefinicao(usuario)
def _usuario_por_token_reset(token)
def esqueci_senha()
def redefinir_senha(token)
def alterar_senha()
def _wants_json()
def _acesso_payload(usuario)
def _json_text(value)  # Coerce optional model fields to a JSON-safe string (never Jinja Undefined).
def _cliente_payload(cliente)  # Payload JSON para modal de clientes no portal (None em chamados = habilitado).
def _pode_gerenciar_clientes(user)
def _rotulo_vinculo_cliente(table_name)
def _excluir_cliente(cliente)  # Remove o cliente após desvincular FKs opcionais; bloqueia se houver dados obriga
def inicio()  # Tela inicial de escolha entre os sistemas
def listar_acessos()
def _exigir_gestao_acessos()
def _exigir_opcao_portal(menu_key, mensagem=None)
def configuracoes()
def _salvar_acesso_geral(usuario, form, novo=False)
def _excluir_acesso_geral(acesso, ator)  # Remove um acesso do sistema, desvinculando FKs opcionais.
def criar_setor_funcao()  # Lista (GET) ou cria (POST) função/setor do catálogo de Acessos.
def setor_funcao_ops(sid)
def novo_acesso()
def editar_acesso(id)
def excluir_acesso(id)
def dashboard()  # Dashboard do sistema de gestão de chamados (widgets com dados reais).
def listar_chamados()  # Lista chamados visíveis ao usuário (técnicos veem todos os abertos).
def ver_chamado(id)  # Ficha do ticket (layout 3 colunas).
def novo_chamado()  # Criar novo chamado
def editar_chamado(id)  # Editar chamado existente
def listar_clientes()  # Lista todos os clientes (cadastro unificado portal/chamados/nutrição).
def novo_cliente()  # Criar novo cliente
def editar_cliente(id)  # Editar cliente existente
def excluir_cliente(id)  # Exclui cliente do cadastro unificado (portal / chamados / nutrição).
def _payload_campanha(chamado)
def api_campanha_ticket()  # Tickets novos e ainda sem atendimento para campainha enquanto técnico/supervisor
def atualizar_status(id)  # API para atualizar status do chamado
def atender_chamado(id)  # Consulta e grava atendimento do técnico (notas, foto, status, encaminhamento).
def _parse_estoque_usos(raw)  # Normaliza lista [{estoque_id, quantidade}] a partir de JSON do formulário.
def _debitar_estoque_finalizar(chamado, atendimento, user, raw_usos)  # Persiste usos e debita ChamadoEstoque.quantidade na mesma transação.
def _redir_usuarios_portal()
def listar_usuarios()  # Usuários do módulo Chamados: centralizado em Acessos na home.
def novo_usuario()  # Usuários do módulo Chamados: centralizado em Acessos na home.
def editar_usuario(id)  # Usuários do módulo Chamados: centralizado em Acessos na home.
def tecnicos()
def _pode_gerenciar_chamado_setor(user)
def _renomear_chamado_setor(sid, nome)
def _excluir_chamado_setor(sid)
def editar_chamado_setor(sid)
def excluir_chamado_setor(sid)
def adicionar_chamado_setor()
def toggle_chamado_setor(sid)
def _tecnico_json(t)
def _parse_funcao_tecnico(data)
def adicionar_chamado_tecnico()
def editar_chamado_tecnico(tid)
def excluir_chamado_tecnico(tid)
def telefones_ramais()
def adicionar_ramal()
def editar_ramal(rid)
def excluir_ramal(rid)
def adicionar_setor_ramal()
def toggle_setor_ramal(sid)  # Alterna ativo/inativo do setor (mesmo ChamadoSetor de técnicos/câmeras).
def _salvar_imagem_camera(arquivo, camera_id=None)  # Salva imagem de câmera em static/uploads/cameras e retorna caminho relativo.
def _remover_imagem_camera(caminho)
def cameras()
def adicionar_setor_camera()  # Cadastro de setor (ChamadoSetor) a partir da página de câmeras.
def toggle_setor_camera(sid)  # Alterna ativo/inativo do setor (mesmo ChamadoSetor dos técnicos).
def adicionar_camera()
def editar_camera(cid)
def excluir_camera(cid)
def _salvar_foto_portao(arquivo, portao_id=None)  # Salva foto de portão em static/uploads/portoes e retorna caminho relativo.
def _remover_foto_portao(caminho)
def portoes()
def adicionar_portao()
def editar_portao(pid)
def excluir_portao(pid)
def adicionar_setor_portao()  # Cadastro de setor (ChamadoSetor) a partir da página de portões.
def toggle_setor_portao(sid)  # Alterna ativo/inativo do setor (mesmo ChamadoSetor de técnicos/câmeras).
def _parse_data_aquisicao(raw)  # Parse YYYY-MM-DD date string; empty → None.
def estoque()
def api_estoque()  # JSON de produtos do estoque para o popup Consultar estoque no atendimento.
def adicionar_estoque()
def editar_estoque(eid)
def excluir_estoque(eid)
def listar_equipamentos()  # Cadastro de equipamentos (patrimônios) vinculados ao cliente.
def adicionar_setor_equipamento()  # Cadastro de setor (ChamadoSetor) a partir da página de equipamentos.
def toggle_setor_equipamento(sid)  # Alterna ativo/inativo do setor (mesmo ChamadoSetor de técnicos/câmeras/ramais).
def _dados_equipamento_form(data)
def api_equipamentos()
def api_equipamento(id)
def _eq_user()
def _link_termo(token)
def api_equipamentos_cronograma()
def api_equipamento_preventiva(id)
def api_equipamento_termo(id)
def api_equipamento_termo_enviar(id)
def imprimir_termo_equipamento(id)
def assinar_termo_publico(token)
def novo_equipamento()  # Criar novo equipamento (popup na listagem; POST legado ainda aceito).
def editar_equipamento(id)  # Editar equipamento existente (popup na listagem).
def auditoria()  # Auditoria dentro do layout de Gestão de Chamados (sidebar preservada).
def _filtro_periodo_chamados()  # Filtro de data_criacao: presets 30/90/ano ou intervalo custom. Default: todos.
def relatorios()  # Catálogo accordion de relatórios (estilo Tiflux).
def _pagina_relatorio_gestao()
def relatorio_gestao()  # Relatório gerencial com dados reais de chamados.
def relatorio_item(slug)
def _pagina_relatorio_atendentes(slug)
def _parse_data_iso(valor)
def _gravar_contrato(form, row=None)
def contratos()
def excluir_contrato(id)
def agenda()
def _sanitize_html(raw)
def conhecimentos()
def novo_conhecimento()
def ver_conhecimento(id)
def _nomes_pastas()
def nova_pasta_conhecimento()
def mensagem_chamado(id)
def atualizar_mesa_chamado(id)
def automacoes()
def equipamentos_por_cliente(cliente_ref)  # Patrimônios vinculados ao cliente selecionado no chamado.
def whatsapp_mensagem()
def api_chamados_whatsapp_status()
def api_chamados_whatsapp_config()
def api_chamados_whatsapp_usuario(uid)
def api_chamados_whatsapp_inbound()

# routes_acesso.py [python]
imports: functools.wraps, datetime.datetime, datetime.date, datetime.time, datetime.timedelta, concurrent.futures.ThreadPoolExecutor, concurrent.futures.as_completed, csv, io, json, logging, os, secrets, re, threading, time, unicodedata, uuid, pathlib.Path, flask.Blueprint, flask.render_template, flask.request, flask.jsonify, flask.redirect, flask.url_for, flask.flash, flask.session, flask.Response, flask.send_file, flask.current_app
def _normalize_tipo(value, default='agente')
def _generate_user_token()
def _login_from_email(email)
def _email_from_usuario(usuario)
def _ensure_usuario_login_fields(u)  # Preenche usuario/token faltantes sem alterar login por e-mail.
def _matriz_chaves_permitidas()
def _perfil_permissoes(perfil)
def _usuario_to_dict(u)
def _fmt_bytes(n)
def _serialize_row(row)
def login_required(f)
def _negar_menu_acesso(mensagem)
def _checar_permissao_menu_acesso()
def _parse_date(value)
def _parse_time(value)
def _horario_from_item(grupo_id, item)  # Monta AcessoHorario a partir de um dict de variação.
def _grupo_payload(grupo)
def _parse_int(value)
def _parse_bool(value, default=False)
def _controlid_server_host_port(data=None)  # Host/porta do servidor de monitor (env, request ou request.host).
def _controlid_mark_online(eq, online=True, device_id=None)
def _apply_probe_results(eqs, timeout=ONLINE_PROBE_TIMEOUT, try_login=False)  # Atualiza eq.online em paralelo (TCP 80/443; login 401 ainda é online).
def _kick_online_probe()  # Dispara probe em background se o TTL expirou (dashboard / listagens).
def _equipamentos_ativos_query()  # Hub / KPIs: só equipamentos ativos (cadastro Sollus).
def _controlid_eqs_alvo(equipamento_id=None, ids=None, only_online=False, only_ativo=True)
def _controlid_direction_for_eq(eq, row=None)
def _controlid_resolve_pessoa(user_id_str)
def _controlid_push_pessoa(eq, pessoa, enviar_fotos=True)  # Envia uma pessoa para o equipamento (user + cartão + foto).
def ensure_acesso_schema()  # Cria tabelas novas e colunas extras em acesso_pessoas (MySQL).
def _seed_lookup(model, field, values)
def ensure_usuarios_acesso_schema()  # Garante colunas usuario/token em usuarios (sem circular import com app).
def _fold_nome_equipamento(value)
def _alinhar_equipamentos_sollus()  # Garante os 6 nomes Sollus e inativa extras de teste (192.168.1.50/51).
def seed_acesso()  # Dados iniciais do módulo de Controle de Acesso.
def _get_backup_config()
def _catalogos()
def _apply_pessoa_fields(pessoa, data)
def _date_range(range_key, custom_date=None)  # Retorna (inicio, fim, label_comparacao, inicio_cmp, fim_cmp).
def _count_status(query)
def _foto_arquivo_estatico(ref: str) -> str  # Se existir JPG/PNG em static/acesso_fotos/{ref}.*, retorna URL web.
def _foto_url_para_exibicao(tipo: str, ref: str, foto_raw: str | None) -> str  # URL leve para UI: path estático direto; data-URL via endpoint.
def _map_fotos_eventos(eventos)  # Batch: pessoa_ref → foto bruta (data-URL ou path) de Pessoa/Visitante.
def _eventos_to_dicts_com_foto(eventos)  # Serializa eventos incluindo foto (URL) com fallback para iniciais no front.
def _ultima_data_com_eventos()  # Retorna a data (date) do evento mais recente, ou None.
def _eventos_filtrados(range_key='today', custom_date=None, status_filter='', search='')
def _dashboard_periodo(range_key='today', custom_date=None, status_filter='', search='', auto_fallback=True)  # Monta query/KPIs do dashboard.
def _stats_periodo(ini, fim)
def dashboard()
def api_dashboard()  # JSON para atualização periódica do monitoramento.
def api_foto_by_ref(tipo, ref)  # Serve foto de pessoa/visitante (data-URL no banco ou arquivo em disco).
def pessoas_page()
def pessoas_export_csv()
def equipamentos_page()
def operacoes_page()
def _operacoes_pessoas_query(args)  # Filtros compartilhados da Central de Operações.
def api_operacoes_pessoas()
def api_operacoes_pessoas_ids()  # IDs de todas as pessoas que batem nos filtros (para >> todos).
def api_operacoes_executar()  # Envia pessoas aos equipamentos Control iD ou atualiza data/hora.
def _parse_int_list(raw)
def _limpeza_ultimo_acesso_map(tipo, equipamento_ids=None)  # pessoa_ref → última data_hora de AcessoEvento para o tipo informado.
def _limpeza_status_pessoa(p)
def _limpeza_filtra_status_pessoa(query, status_filtro)
def _limpeza_filtra_status_visitante(query, status_filtro)
def _limpeza_analisar(data)  # Pessoas/visitantes sem evento de acesso há X dias (AcessoEvento.data_hora).
def limpeza_page()
def api_limpeza_analisar()
def api_limpeza_executar()  # Stub de limpeza: opcionalmente desativa no banco e remove vínculos.
def _sync_offline_status_filter(query, status)
def _sync_offline_evento_existe(eq_id, pessoa_ref, data_hora, status, direction)  # Evita duplicar eventos já persistidos (chave aproximada).
def sync_offline_page()
def api_sync_offline_coletar()  # Coleta access_logs do equipamento Control iD e cruza com AcessoEvento.
def api_sync_offline_salvar()  # Persiste eventos selecionados em AcessoEvento se ainda não existirem.
def grupos_page()
def visitantes_page()
def escalas_page()
def pessoas_veiculos_page()
def _pessoa_veiculo_dict(p, veiculos_count=0)
def _normalize_placa(value)
def _veiculo_from_payload(data, item=None, pessoa_id=None)
def api_pessoas_veiculos_pessoas()  # Lista proprietários (AcessoPessoa) com paginação e KPIs da frota.
def api_pessoa_veiculos(pid)
def api_pessoa_veiculo_item(vid)
def _escala_counts_map(ids=None)
def _escala_to_dict(item, incluir_pessoas=False)
def _escala_from_payload(data, item=None)
def _query_escalas_filtrada()
def api_escalas()
def api_escala_item(eid)
def _escala_api_get(eid)
def _pessoa_por_token(raw)  # Resolve colaborador por PK inteiro, matrícula ou documento (CPF).
def _pessoas_from_escala_payload(data)
def _escala_pessoas_payload(item)
def api_escala_pessoas(eid)
def api_escala_pessoa_item(eid, pid)
def _parse_date_arg(value)
def _relatorio_eventos_query(
    inicio=None,
    fim=None,
    sentido='',
    status='',
    q='',
    tab='',
    equipamento_id='',
    event_type='',
)  # Filtro compartilhado da página Relatório de Eventos e do export CSV.
def _kpis_eventos(query)
def _eventos_filter_args()
def eventos_page()
def eventos_export_csv()
def ambientes_page()
def _parse_publico(value)
def _normalize_fluxo(value)
def _sync_ambiente_equipamentos(ambiente, vinculos)  # Substitui vínculos equipamento+fluxo do ambiente.
def _apply_ambiente_payload(ambiente, data, creating=False, sync_equipamentos=True)
def api_ambientes()
def api_ambiente_item(aid)
def _estacionamento_permissoes_opcoes()
def _normalize_est_fluxo(value)
def _sync_estacionamento_equipamentos(est, vinculos)
def _sync_estacionamento_permissoes(est, vinculos)
def _apply_estacionamento_payload(est, data, creating=False)
def estacionamentos_page()
def api_estacionamentos()
def api_estacionamento_item(eid)
def impressoras_page()
def _apply_impressora_payload(imp, data, creating=False)
def _ensure_unico_padrao_voucher(imp)  # Garante no máximo uma impressora marcada como padrão para vouchers.
def api_impressoras()
def api_impressora_item(iid)
def controle_adicional_page()
def parametros_refeicoes_page()
def parametros_documentos_page()
def _veiculos_periodo_bounds(periodo)  # Retorna (ini, fim) para filtros do relatório de veículos.
def _veiculos_filter_args()
def _veiculos_query(f)  # Query AcessoVeiculoEvento com filtros; lista vazia se tabela sem dados.
def veiculos_page()
def veiculos_export_csv()
def _parse_datetime_arg(value, end_of_day=False)  # Aceita datetime-local, data+hora ou só data (YYYY-MM-DD).
def _fmt_datetime_local(dt)
def _fmt_duracao_hms(seconds)
def _evento_local(evento, eq_local_map)
def _is_entrada(direction)
def _is_saida(direction)
def _permanencia_person_key(evento)
def _permanencia_filter_args()
def _permanencia_eventos_query(f)  # Eventos liberados de entrada/saída no período (base do cálculo de permanência).
def _lookup_permanencia_cadastros(pessoa_refs)  # Mapas matricula/visitor_id → dados org para enriquecer linhas.
def _enrich_org(evento, pessoas_map, visitantes_map)
def _match_org_filters(org, f)
def _build_permanencia_rows(eventos, f)  # Pareia Entrada→Saída por pessoa_ref/nome e devolve linhas do relatório.
def _permanencia_row(entrada, saida, eq_local_map, pessoas_map, visitantes_map)
def _permanencia_rows_for_request()
def permanencia_page()
def permanencia_export_csv()
def _format_detalhes_json(detalhe)  # Normaliza detalhe para bloco JSON legível na UI.
def _seed_acesso_auditoria_demo()  # Se não houver logs do módulo acesso, cria exemplos para demonstração.
def auditoria_page()
def _fmt_brl(valor)
def _refeicoes_filter_args()  # Lê filtros comuns da análise de refeições.
def _refeicoes_agregar(rows, group_attr, label_key)  # Agrupa refeições por atributo → volume, ticket médio e total.
def _refeicoes_query(tipo_pessoa='todos', q='', de='', ate='')  # Filtra AcessoRefeicao; retorna query ordenada (mais recente primeiro).
def _refeicoes_kpis(tipo_pessoa='todos')
def refeicoes_page()
def refeicoes_export()
def refeicoes_imprimir()  # Visão de impressão/PDF — relatório gerencial sem sidebar.
def sobre_page()
def usuarios_page()
def permissoes_page()
def backup_page()
def documentacao_page()
def sollus_sync_page()
def api_backup_agendamento()
def api_usuarios()
def api_usuarios_item(uid)
def api_permissoes(usuario_id)  # API legada: permissões por usuário (mantida por compatibilidade).
def api_permissoes_perfil(perfil)
def api_backup_gerar()
def backup_download(bid)
def empresas_page()
def classificacoes_page()
def cadastros_diversos_page()
def departamentos_page()
def setores_page()
def centros_custo_page()
def tipos_documento_page()
def _api_lookup_list_create(model, field='descricao', unique=True, transform=None)  # GET lista / POST cria para cadastros auxiliares simples.
def _api_lookup_item(model, item_id, field='descricao', unique=True, transform=None)
def _empresa_payload()  # Lê JSON ou multipart/form-data (com logo opcional).
def _save_empresa_logo(upload, empresa_id)  # Salva logo em static/acesso_empresas/ e retorna path relativo a static/.
def _delete_empresa_logo_file(logo_path)
def api_consultar_cnpj()  # Consulta pública de CNPJ (BrasilAPI) para preencher razão social.
def api_empresas()
def api_empresas_item(eid)
def _normalize_classificacao_perfil(raw)  # Normaliza perfil_fixo: None/'', 'sem_perfil' → None; senão chave TIPO_LABELS.
def _tf_classificacao(data)
def api_classificacoes()
def api_classificacoes_item(cid)
def api_departamentos()
def api_departamentos_item(did)
def api_setores()
def api_setores_item(sid)
def api_centros_custo()
def api_centros_custo_item(cid)
def api_locais()
def api_locais_item(lid)
def api_pessoas()
def api_pessoa_item(pid)
def api_equipamentos()
def api_equipamentos_acoes()  # Ações do Hub: configurar servidor / reiniciar / probe Control iD.
def api_equipamentos_status()  # Probe paralelo (TCP 80/443) e devolve a lista com online atualizado.
def api_equipamentos_atualizar_data_hora()  # Envia data/hora aos equipamentos via set_system_time.fcgi.
def api_equipamentos_reiniciar_conexao()  # Reloga e reaplica monitor em todos os equipamentos ativos com IP.
def api_equipamentos_puxar_device_id()
def api_equipamento_item(eid)
def _controlid_find_eq_by_device(device_id)
def _controlid_ingest_access_log_values(values, device_id=None)  # Persiste um access_log vindo do monitor dao.
def controlid_device_is_alive()
def controlid_notifications_dao()
def controlid_notifications_misc()
def api_grupos()
def api_grupo_item(gid)
def _apply_visitante_fields(visitante, data, creating=False)
def api_visitantes()
def api_visitante_item(vid)
def api_visitantes_buscar()  # Busca por documento ou nome (reutiliza cadastro existente).
def api_visitantes_anfitrioes()  # Autocomplete de anfitrião a partir de colaboradores.
def api_tipos_documento()
def api_tipos_documento_item(tid)
def _hub_docs_filter_pessoas()  # Filtros comuns do Hub de Documentos.
def _pessoa_hub_dict(p)
def api_hub_documentos_pessoas()
def _save_documento_upload(upload, pessoa_id)  # Salva arquivo em static/acesso_documentos/ e retorna o nome do arquivo.
def _delete_documento_arquivo(filename)
def api_hub_pessoa_documentos(pid)
def api_hub_documento_item(did)
def api_eventos()
def _controle_adicional_from_payload(data, item=None)
def api_controles_adicionais()
def api_controle_adicional_item(cid)
def _parse_valor_refeicao(value)
def _item_refeicao_from_payload(data, item=None, grupo_id=None)  # Cria/atualiza item. Aceita hora_inicio > hora_fim (cruza meia-noite).
def _grupo_refeicao_from_payload(data, grupo=None)
def api_grupos_refeicao_pessoas_busca()  # Busca pessoas para vínculo em grupos de refeição.
def api_grupos_refeicao()
def api_grupo_refeicao_item(gid)
def api_grupo_refeicao_itens(gid)
def api_item_refeicao(iid)
def api_grupo_refeicao_vinculos(gid)
def api_grupo_refeicao_pessoas_disponiveis(gid)  # Pessoas ativas ainda não vinculadas a este grupo de refeição.
def api_grupo_refeicao_vinculos_remover(gid)  # Remove vínculos selecionados (por id de vínculo ou pessoa_id).
def api_grupo_refeicao_vinculos_por_grupo(gid)  # Add/Remover em lote a partir de outro grupo de acesso ou de refeição.
def api_vinculo_refeicao(vid)
def api_grupos_acesso_lista()  # Lista simples de grupos de acesso (para Add/Remover por Grupo).

# routes_audit.py [python]
imports: datetime.date, datetime.timedelta, flask.Blueprint, flask.render_template, flask.request, flask.jsonify, flask.redirect, flask.url_for, flask.session, flask.flash, functools.wraps, audit_service.listar_logs, audit_service.ensure_audit_table, nutricao_service._parse_date
def login_required(f)
def active(page)
def dashboard()
def api_logs()

# routes_logistica.py [python]
imports: json, datetime.datetime, functools.wraps, flask.Blueprint, flask.flash, flask.jsonify, flask.redirect, flask.render_template, flask.request, flask.session, flask.url_for, logistica_service.CAMPOS_DESCONTOS, logistica_service.CAMPOS_EMPRESA, logistica_service.CAMPOS_PROVENTOS, logistica_service.CATEGORIAS_LANCAMENTO, logistica_service.FUNCOES_DP, logistica_service.STATUS_ENTREGA, logistica_service.STATUS_MANUTENCAO, logistica_service.STATUS_REVISAO, logistica_service.STATUS_ROTA, logistica_service.TIPOS_CHECKLIST, logistica_service.TIPOS_SERVICO, logistica_service.alertas_dashboard, logistica_service.custo_operacional, logistica_service.custo_por_veiculo, logistica_service.evolucao_custos, logistica_service.geocodificar_endereco, logistica_service.km_da_polyline, logistica_service.periodo_padrao, logistica_service.polyline_entre_pontos, logistica_service.retracar_rotas_incompletas, logistica_service.seed_logistica, logistica_service.sincronizar_pontos_clientes
def login_required(f)
def _checar_permissao_menu_logistica()
def _parse_float(value)
def _parse_int(value)
def _parse_date(value)
def _parse_bool(value, default=True)
def _json_body()
def _placa_de_veiculo(veiculo_id, placa)
def _page(template, active, **ctx)
def dashboard()
def lancamentos_page()
def frota_page()
def rotas_page()
def manutencao_page()
def revisoes_page()
def entregas_page()
def dp_page()
def folgas_page()
def ociosidade_page()
def operacao_page()
def checklist_page()
def higiene_page()
def coletas_page()
def auditoria()
def api_resumo()
def _lista_veiculos()
def api_veiculos()
def api_veiculo(item_id)
def api_lancamentos()
def api_lancamento(item_id)
def api_manutencoes()
def api_manutencao(item_id)
def api_revisoes()
def api_revisao(item_id)
def _clientes_para_pontos()
def _pontos_por_id()
def _rota_to_dict(item, pontos=None)
def _status_rota(value, default='Pendente')
def _preencher_rota_pontos(item, d, geocode=None, route_fetch=None)
def api_rotas()
def api_rota(item_id)
def _lat_lng_do_endereco(d, endereco, lat=None, lng=None)
def api_geocode()
def _aplicar_cliente_no_ponto(d, nome, endereco)
def api_sincronizar_clientes()
def _payload_mapa(resumo=None, rotas_retracadas=0)
def api_mapa()
def api_preparar_mapa()
def api_entregas()
def api_entrega(item_id)
def api_colaboradores()
def api_colaborador(item_id)
def api_folhas()
def api_folha(item_id)
def api_folgas()
def api_folga(item_id)
def api_ociosidade()
def api_ociosidade_item(item_id)
def api_checklists()
def api_checklist(item_id)
def api_higienes()
def api_higiene(item_id)
def api_coletas()
def api_coleta(item_id)

# routes_nutricao.py [python]
imports: flask.Blueprint, flask.render_template, flask.request, flask.redirect, flask.url_for, flask.flash, flask.jsonify, flask.session, flask.g, flask.abort, functools.wraps, datetime.datetime, datetime.date, json, os, sqlalchemy.exc.IntegrityError, models.db, models_nutricao.NutClinica, models_nutricao.NutGrupoClinica, models_nutricao.NutEnfermaria, models_nutricao.NutLeito, models_nutricao.NutDieta, models_nutricao.NutGrupoDieta, models_nutricao.NutCategoriaDieta, models_nutricao.NutPaciente, models_nutricao.NutMapaRefeicao, models_nutricao.NutCardapio, models_nutricao.NutTabelaNutrientes, models_nutricao.NutAlimento, models_nutricao.NutAlimentoNutriente, models_nutricao.NutPratoLiquido, models_nutricao.NutEstoqueLocal, models_nutricao.NutUnidadeMedida, models_nutricao.NutGrupoProduto, models_nutricao.NutProduto, models_nutricao.NutFornecedor, models_nutricao.NutEtiqueta, models_nutricao.NutEtiquetaCampo, models_nutricao.NutPrecoRefeicao, models_nutricao.NutTipoRefeicao, models_nutricao.NutPrecoDietaTipo, models_nutricao.NutRefeicaoAcompanhante, models_nutricao.NutRefeicaoFuncionario
def _nutricao_tenant_gate()
def _inject_nutricao_cliente()
def active(page)
def fmt_data(d)
def _list_pacientes_db(ativos_only=True, q=None, limit=50)
def _usuario_sessao()
def _list_dietas_db(somente_ativas=True)
def _list_clinicas_db(somente_ativas=False)
def _mapa_linhas(data_ref)
def _get_scoped_or_404(model, pk)
def _get_scoped(model, pk)
def _tag_cliente(obj)
def dashboard()
def pacientes()
def api_pacientes()
def api_paciente(pid)
def _resolve_dieta_refeicao(d)  # Retorna (dieta_id, refeicao_nome) a partir de dieta_id e/ou refeicao/dieta texto
def _list_acompanhantes_db(ativos_only=True, q=None, data_ref=None, paciente_id=None)
def _list_funcionarios_refeicao_db(ativos_only=True, q=None, data_ref=None)
def _parse_bool_flag(d, key, default=True)
def refeicao_acompanhante()
def api_refeicao_acompanhantes()
def api_refeicao_acompanhante_ops(aid)
def refeicoes_funcionarios()
def _parse_quantidade_refeicao(d, default=1)
def api_refeicao_funcionarios()
def api_refeicao_funcionario_ops(fid)
def api_mapa_get()
def api_mapa_inserir()  # Insere paciente existente (ou atualiza cadastro) no mapa do dia.
def api_mapa_novo_lancamento(mid)  # Cria nova dieta ou substituição vinculada ao mesmo paciente/leito.
def api_mapa_avisos_alta()
def _seed_aviso_alta_demo(data_ref)  # Demo desativada: não altera pacientes reais (evita sumiço indevido no mapa).
def api_mapa_put(mid)
def api_mapa_saida(mid)  # Alta médica, óbito ou transferência externa — baixa com histórico (não apaga a l
def api_mapa_toggle(mid)
def api_mapa_substituicoes(mid)  # Cardápio padrão + substituições (cardápio personalizado) da linha do mapa.
def api_mapa_substituicoes_importar(mid)  # Importa pares ou justificativa do mapa anterior do mesmo paciente.
def api_mapa_substituicoes_justificativas(mid)  # Lista justificativas anteriores do mesmo paciente (para popup de importação).
def _filtro_nome_imprimir(imprimir_por, filtro_id, filtro_nome)
def clinicas()
def api_clinicas()
def api_clinica_ops(cid)
def api_clinica_enfermarias(cid)
def _grupo_clinica_removido()
def grupos_clinicas()
def api_grupos_clinicas()
def api_grupo_clinica_ops(gid)
def api_grupo_clinica_clinicas(gid)
def enfermarias()
def api_enfermarias()
def api_enfermaria_ops(eid)
def api_enfermaria_leitos(eid)
def leitos()
def api_leitos()
def api_leito_ops(lid)
def _parse_precos_payload(d)  # Extrai mapa de preços do JSON (precos / valores por tipo ou sigla).
def dietas()
def api_dietas()
def api_dieta_ops(did)
def categorias_dietas()
def api_categorias_dietas()
def api_categoria_dieta_ops(cid)
def grupos_dietas()
def api_grupos_dietas()
def api_grupo_dieta_ops(gid)
def tipos_refeicao()
def api_tipos_refeicao()
def api_tipo_refeicao_ops(tid)
def _resolve_cardapio_dieta(d)  # Resolve dieta_id + nome. Hook futuro: join NutPrecoDietaTipo via dieta_id × hr_*
def _cardapio_from_payload(d, row=None)
def cardapios()
def api_cardapios()
def api_cardapio_ops(cid)
def _fnum(v, default=0.0)
def _alimento_from_payload(d, row=None, tabela_id=None)
def _sync_nutrientes(alimento, nutrientes)
def nutricional()
def api_tabelas_nutrientes()
def api_tabela_nutrientes_ops(tid)
def api_import_fdc_tabela()  # Importa ZIP/JSON FoodData Central Foundation Foods (multipart file ou path).
def api_alimentos()
def api_alimento_ops(aid)
def _prato_liquido_from_payload(d, row=None)
def pratos_liquidos()
def api_pratos_liquidos()
def api_prato_liquido_ops(pid)
def _fnum_prod(v, default=0.0)
def _produto_from_payload(d, row=None)
def produtos()
def api_estoques()
def _unidade_from_payload(d, row=None)
def api_unidades()
def api_unidade_ops(uid)
def api_grupos_produto()
def api_grupo_produto_ops(gid)
def api_produtos_cadastro()
def api_cardapio_produtos()  # Produtos do cardápio: grupo = tema do campo (Acompanhamento, Bebida, …).
def api_produto_cadastro_ops(pid)
def _fornecedor_from_payload(d, row=None)
def fornecedores()
def api_fornecedores()
def api_fornecedor_ops(fid)
def _fnum_eti(v, default=0.0)
def _etiqueta_from_payload(d, row=None)
def _sync_etiqueta_campos(etiqueta, campos)
def etiquetas()
def impressao_etiquetas()
def impressao_etiquetas_imprimir()
def impressao_mapa()
def impressao_mapa_imprimir()
def impressao_mapa_distribuicao()
def impressao_mapa_distribuicao_imprimir()
def api_etiquetas()
def api_etiqueta_ops(eid)
def precos_refeicoes()
def _parse_money(val, fallback=0.0)
def api_precos_refeicoes()
def api_precos_refeicoes_adicionar()  # Adiciona/atualiza valores de refeição para uma dieta no contexto
def mapa_refeicoes()
def estoque()
def api_produtos_legado()
def relatorios()
def relatorio_mapa_uma()
def api_relatorio_mapa_uma()
def totalizacao_dietas()
def totalizacao_dietas_imprimir()
def totalizacao_dietas_refeicoes()
def totalizacao_dietas_refeicoes_imprimir()
def relatorio_mapa_uma_imprimir()
def _seed_mapa_uma_demo(data_ref)  # Preenche campos U.M.A. de exemplo se o mapa do dia estiver vazio nesses campos.
def faturamento()
def faturamento_valores()
def faturamento_valores_imprimir()
def faturamento_valores_exportar()
def _params_faturamento_from_request(src)
def faturamento_imprimir()
def faturamento_exportar()
def _seed_faturamento_demo(data_de, data_ate)  # Garante flags de refeição e mix de dietas para demonstrar o espelho.
def auditoria()  # Auditoria dentro do layout de Nutrição Hospitalar (sidebar preservada).
def admin()
def api_usuarios_create()
def utilitarios()
def api_totalizacao_direta()
def api_autorizacoes_substituicao()
def api_logs()

# routes_pesagem.py [python]
imports: functools.wraps, datetime.date, datetime.datetime, datetime.timedelta, pathlib.Path, os, socket, uuid, flask.Blueprint, flask.render_template, flask.request, flask.jsonify, flask.redirect, flask.url_for, flask.flash, flask.session, flask.send_file, sqlalchemy.func, werkzeug.utils.secure_filename, models.db, models.Usuario, models_pesagem.PesagemBalanca, models_pesagem.PesagemCliente, models_pesagem.PesagemLeitura, models_pesagem.PesagemWhatsAppDestino, models_pesagem.PesagemWhatsAppEnvio
def login_required(f)
def _checar_permissao_menu_pesagem()
def _check_api_key()
def api_key_required(f)
def _agente_ou_login()  # AgentePesagem.exe (X-API-Key) ou sessão web.
def seed_pesagem()
def _agente_pesagem_version() -> str
def _agente_exe_download_name() -> str
def download_agente_arquivo(nome)  # Serve o .exe/.json/.bat publicados por build_exe.bat, sem cache do navegador.
def _imagem_url_cliente(cliente)  # URL da foto do Cadastro de Cliente (pesagem). Relativa para o agente prefixar o 
def _cliente_to_dict(cliente)
def _apagar_arquivo_cliente(rel_path)
def _salvar_imagem_cliente(upload, cliente_id)
def _resolver_cliente_leitura(d)  # Resolve cliente cadastrado (pesagem_clientes) a partir do agente.
def _resolver_data_leitura(d)
def _parse_float(value)
def _parse_date_arg(value)
def _filtros_leituras_from_request(args=None)  # Lê filtros da query string para o dashboard / impressão.
def _aplicar_filtros_leituras(query, filtros)
def _rotulo_filtros(filtros, cliente_nome=None)
def _resumo_leituras(filtros)
def _consultar_dashboard(filtros)
def dashboard()
def dashboard_imprimir()  # Impressão das leituras com os mesmos filtros da tela.
def balancas_page()
def clientes_page()
def whatsapp_page()
def auditoria()  # Auditoria dentro do layout de Controle de Pesagem (sidebar preservada).
def api_health()
def api_receber_leitura()  # Recebe peso do agente Windows (.exe) ou integração.
def api_listar_leituras()  # Lista lançamentos. A tela web usa a sessão; o agente usa X-API-Key.
def api_excluir_leitura(lid)  # Remove um lançamento de pesagem da listagem (dashboard ou agente).
def api_editar_leitura(lid)  # Altera cliente e/ou líquido de um lançamento (dashboard ou agente).
def api_balancas()
def api_balanca(bid)
def api_listar_clientes()  # Lista o Cadastro de Cliente da pesagem (tabela pesagem_clientes), não os cliente
def api_criar_cliente()
def api_cliente(cid)
def _destino_payload()
def api_whatsapp_status()
def api_whatsapp_logout()
def api_whatsapp_preview()
def api_whatsapp_destinos()
def api_whatsapp_destino(did)
def api_whatsapp_enviar(did)

# run_demo.py [python]
imports: flask.Flask, config_demo.Config, models.db, routes.main
def create_app()

# scripts/importar_fotos_equipamentos.py [python]
imports: base64, json, sys, urllib, pathlib.Path
def _post_json(url: str, payload: dict | None = None, timeout: float = 12)
def login(ip: str, user: str, password: str) -> str | None
def list_user_ids(ip: str, session: str) -> list[str]
def get_image(ip: str, session: str, user_id: str) -> bytes | None
def devices_from_db()
def main()

# scripts/importar_sollus_acesso.py [python]
imports: argparse, base64, mimetypes, re, sys, datetime.datetime, datetime.date, datetime.time, pathlib.Path
def _parse_sql_values(values_blob: str) -> list[tuple]  # Parsea tuplas de um INSERT MySQL (suporta NULL, números, strings com aspas).
def _find_statement_end(text: str, values_kw: int) -> int  # Encontra o ';' que fecha o INSERT, ignorando ponto-e-vírgula dentro de strings.
def _iter_inserts(text: str, table: str)  # Yields (cols|None, rows) for every INSERT INTO `table` in the dump.
def _extract_insert(text: str, table: str) -> tuple[list[str] | None, list[tuple]]  # Retorna (colunas ou None, rows) do primeiro INSERT da tabela.
def _extract_all_insert_rows(text: str, table: str) -> tuple[list[str] | None, list[tuple]]  # Concatena todos os INSERT da tabela (necessário para `eventos`).
def _row_dict(cols: list[str] | None, row: tuple, fallback_cols: list[str]) -> dict
def _parse_date(val) -> date | None
def _parse_time(val) -> time | None
def _find_photo(filename: str | None) -> str | None  # Retorna data-URL se achar o arquivo; senão None.
def _status_pessoa(status_val, deleted_at) -> tuple[str, bool]
def _parse_datetime(val) -> datetime | None
def _tipo_pessoa_from_dump(tipo_usuario, classificacao) -> str
def _is_visitante_user(d: dict) -> bool
def _load_table_from_db(conn, table: str) -> tuple[list[str], list[tuple]]
def _iter_eventos_from_db(conn, since: datetime | None = None, batch_size: int = 5000)
def _decode_dump(dump_path: Path) -> str
def importar(
    dump_path: Path | None = None,
    replace_eventos: bool = False,
    batch_size: int = 2000,
    from_db: dict | None = None,
) -> dict
def main()

# scripts/run_migrations.py [python]
imports: hashlib, os, sys, datetime.datetime
def log(msg)
def file_checksum(path)
def is_sqlite(engine)
def ensure_table(engine)
def applied_filenames(engine)
def mark_applied(engine, filename, checksum)
def split_sql(sql)
def run_sql_file(engine, path)
def run_py_file(engine, path)
def list_migration_files()
def load_app_and_db()
def main()

# static/js/chamado-campanha.js [javascript]
function gravar(id)
function idsPendentes()
function persistirPendentes()
function aindaPendente(item)
function marcarPendente(item)
function limparPendente(id)
function sincronizarPendentes(vivos, consultados)
function tom(ctx, freq, start, dur, vol)
function gravarSomLiberado()
function criarAudio()
function comAudioPronto(cb)
function ouvirGestoParaSom()
function tocarCampainha(forcar)
function pararLembrete()
function garantirLembrete()
function garantirEstilo()
function ocultarPedidoSom()
function mostrarPedidoSom()
function ativarSom(ev)
function atualizarChip()
function garantirCard()
function mostrar(item)
function fechar()
function enfileirar(itens)
function consultar()
function iniciar()

# static/js/chamado-setores.js [javascript]
function $(id)
function esc(s)
function acoesHtml(id)
function rowHtml(d)
function selects(ids)
function appendIdOption(sel, id, nome)
function appendNomeOption(sel, nome, oldNome)
function removeIdOption(sel, id)
function removeNomeOption(sel, nome)
function setTitle(text)
function showErro(msg)
function abrirModal()
function fecharModal()
function nomeDaLinha(id)
function syncAdded(d)
function syncRenamed(d, oldNome)
function syncDeleted(id, nome)
function ensureTbody()
function novo()
function editar(id)
function salvar()
function excluir(id)
function init(options)

# static/js/chamados-mdk.js [javascript]
function toast(msg)

# static/js/loading-overlay.js [javascript]
function ensureOverlay()
function setMessages(message, sub)
function showAppLoading(message, sub)
function hideAppLoading(force)
function bumpFetch(delta)
function isModifiedClick(ev)
function isFileDownloadHref(href)
function shouldSkipLink(a)
function onDocumentClick(ev)
function onDocumentSubmit(ev)
function wrapFetch()
function wrapXHR()
function resetStuck()

# static/js/logistica.js [javascript]
function money(n)
function escapeHtml(str)
function val(row, key)
function listUrl()
function load()
function optionsVeiculo(selected)
function optionsColab(selected)
function optionsCat(selected)
function optionsExtra(selected)
function labelPonto(p)
function optionsPonto(selected)
function refreshPontos()
function syncNomeRota()
function fieldHtml(field, row)
function readForm()
function openForm(row)

# static/js/main.js [javascript]
function showAlert(message, type = 'info')
function confirmarAcao(mensagem = 'Tem certeza que deseja realizar esta ação?')
function formatarData(data)
function formatarMoeda(valor)

# update_passwords.py [python]
imports: app.create_app, models.db, models.Usuario, password_utils.generate_password_hash

# whatsapp_bridge/index.js [javascript]
function writePid()
function clearPid()
function loadLibs()
function findChrome()
function chromeMajorVersion(chromePath)
function buildUserAgent(chromePath)
function toJid(phone)
function isDirectChat(jid)
function inboundToken()
function resolvePhone(jid)
function handleIncoming(msg, source)
function clearQr()
function tryReadUserFromStore(sock)
function refreshUser(sock)
function markOpen(sock)
function hydrateStatus()
function bindClientEvents(sock)
function startClient()
function chromeMissingMessage(msg)
function wipeAuthDir()
function logoutAndRestart()
function postInbound(payload)
function json(res, code, payload)
function readBody(req)
function statusPayload()
function shutdown()

# whatsapp_chamados.py [python]
imports: json, logging, re, datetime.datetime, models.Chamado, models.ChamadoMensagem, models.ChamadoSetor, models.Cliente, models.Equipamento, models.Usuario, models.WhatsAppChamadoConfig, models.WhatsAppChamadoLog, models.WhatsAppChamadoUsuario, models.aplicar_automacoes, models.contrato_vigente, models.db, models.mesa_padrao, whatsapp_pesagem.normalizar_telefone
def saudacao(agora=None)
def config_ativa()
def listar_clientes()
def listar_setores()
def parse_indice(texto, maximo)
def parse_sim_nao(texto)
def gravar_lista(usuario, tipo, ids)
def ler_lista(usuario, tipo)
def formatar_lista(titulo, itens)
def primeiro_nome(nome)
def _log(usuario, direcao, texto)
def obter_usuario(telefone)
def _tecnico_bot()
def abrir_ticket(usuario, patrimonio, defeito=None)
def _encontrar_equipamento(codigo)
def _guardar_patrimonio(usuario, codigo)
def _patrimonio_pendente(usuario)
def _gerar_os()
def _pedir_clientes(usuario)
def _pedir_setores(usuario)
def _msg_confirmacao(usuario)
def _msg_patrimonio()
def _msg_defeito()
def _vincular_por_indice(usuario, texto, tipo)
def process_inbound(telefone, texto, sender=None, agora=None, jid=None)  # Processa uma mensagem recebida no número logado. Retorna as respostas enviadas.

# whatsapp_pesagem.py [python]
imports: json, logging, os, secrets, shutil, socket, subprocess, threading, time, urllib, zipfile, datetime.date, datetime.datetime, datetime.timedelta, pathlib.Path, requests, sqlalchemy.func, models.db, models_pesagem.PesagemLeitura, models_pesagem.PesagemWhatsAppDestino, models_pesagem.PesagemWhatsAppEnvio
def formatar_kg(valor)
def normalizar_telefone(raw)
def telefone_valido(raw)
def parse_hora(value)
def peso_liquido_expr()
def totais_liquido_do_dia(dia=None)  # Soma o peso líquido de cada cadastro no dia (ex.: 10 pesagens CCD → um total).
def montar_linhas_totais(totais)
def montar_mensagem(template, totais=None, dia=None)
def _port_open(host, port, timeout=0.4)
def _portable_node()
def _which_node()
def _which_npm()
def inbound_token()
def _inbound_url()
def _bridge_env()
def _npm_argv()
def ensure_node_runtime()  # Usa Node do sistema ou baixa um portátil (Windows) sem instalação de administrad
def _kill_pid(pid)
def _pids_listening(port)
def _stop_bridge_process()
def _bridge_health_engine()  # web', 'legacy' ou None se a porta ainda não responde HTTP.
def _web_lib_installed()
def ensure_bridge_installed()
def start_bridge_process()  # Sobe o processo Node (WhatsApp Web no Chrome) se a porta ainda não estiver em us
def _disabled()
def _bridge_unavailable_reason()
def _bridge_get(path, timeout=8)
def _bridge_post(path, payload=None, timeout=45)
def status_whatsapp(auto_start=True)
def logout_whatsapp()
def send_whatsapp(telefone, texto)
def _ja_enviou(destino_id, dia, tipo)
def _gravar_envio(destino, dia, tipo, status, corpo, erro=None)
def enviar_para_destino(destino, tipo='manual', dia=None, sender=None)
def process_scheduled_sends(now=None, sender=None)  # Envia uma vez ao dia no horário cadastrado de cada destinatário ativo.
def ultimo_envio_por_destino(destino_ids)
def _scheduler_loop(app)
def start_background(app)  # Inicia ponte Node + thread do horário (uma vez por processo).

# wsgi.py [python]
imports: app.create_app, app.ensure_pesagem_schema, app.ensure_logistica_schema, models.db, whatsapp_pesagem.start_background
